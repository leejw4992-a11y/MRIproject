# 노트북 셀별 해설 — 6_lgg_segmentation.ipynb

발표 준비용. 각 셀마다 세 가지로 정리했다.

- **무엇을** — 이 셀이 하는 일
- **왜** — 이렇게 짠 이유 (발표의 핵심)
- **질문받으면** — 예상 질문과 답

`★` 표시는 질문 가능성이 높은 셀.
`(미실행)` 표시는 아직 돌리지 않은 구간.

---

# 0. 준비

## 0-1. import + device ★

**무엇을**
필요한 라이브러리를 불러오고, 시드를 고정하고, GPU를 잡는다.

**왜**
2~5번 노트북의 device 코드는 `torch.cuda.is_available()`만 확인한다.
맥에서 열면 조용히 CPU로 떨어지므로 `hasattr` 가드와 MPS 분기를 추가했다.
GPU 이름과 VRAM을 출력하는 것도 추가했다 — `cu128` 같은 CUDA **빌드** 표시는
GPU가 실제로 잡혔다는 뜻이 아니기 때문이다.

`accuracy_score`를 import 목록에서 뺐다. 4번에서는 썼지만 여기서는 쓰면 안 된다(7번 셀 참조).

**질문받으면**
> Q. 시드를 왜 세 군데나 고정했나요?
> A. `torch.manual_seed`는 모델 가중치 초기화, `np.random.seed`는 numpy 연산,
>    `SegDataset` 내부의 `default_rng(seed)`는 증강과 빈 슬라이스 샘플링을 담당합니다.
>    세 개가 독립적이라 하나만 고정하면 재현이 안 됩니다.

## 0-2. 하이퍼파라미터 ★

**무엇을**
`IMAGE_SIZE=256`, `BASE=32`, `BATCH_SIZE=16`, `EPOCHS=30`, `NEG_RATIO=1.0` 등을 한곳에 모은다.

**왜**
`IMAGE_SIZE`를 256에서 내리지 않는 이유가 핵심이다.
이 데이터의 **최소 종양이 7픽셀**이라 128로 줄이면 2픽셀이 되고,
마스크를 nearest로 리사이즈하는 과정에서 아예 사라지는 슬라이스가 생긴다.
속도를 아끼려다 정답을 훼손하는 셈이다.

`BASE=32`는 속도 측정 결과(에폭 16초)와 데이터 양(환자 78명)을 함께 고려한 값이다.
`base=64`는 34.5M 파라미터라 이 데이터 양에 과하다.

`USE_AMP`는 CUDA일 때만 켜진다. 혼합정밀도로 1.5~2배 빨라진다.

**질문받으면**
> Q. 배치를 32로 키우면 더 빠르지 않나요?
> A. 아닙니다. 벤치마크에서 스텝당 시간이 배치에 정확히 비례했습니다
>    (batch 8: 67.5ms, batch 16: 137ms). GPU가 이미 포화 상태라 에폭 시간은 같고,
>    파라미터 갱신 횟수만 절반(3,570 → 1,800회)으로 줄어듭니다.

## 0-3. 실행 환경 기록

**무엇을**
OS, 파이썬, torch, CUDA, GPU, 라이브러리 버전을 `env.json`으로 저장한다.

**왜**
재현성 확보용. 보고서에 "이 환경에서 돌렸다"를 쓸 때 그대로 인용한다.

---

# 1. 데이터 인덱싱

## 1-0. 경로 탐색 + 무결성 검증 ★

**무엇을**
`kaggle_3m` 폴더를 찾고, 환자 110명 / 원본 3,929 / 마스크 3,929를 검증한 뒤
파일 목록을 데이터프레임으로 만든다.

**왜**
`glob`으로 **마스크를 먼저** 잡고 `_mask`를 떼어 원본 경로를 만든다.
원본을 먼저 잡으면 `*.tif` 패턴에 마스크까지 걸려서 걸러내야 한다.

기댓값과 다르면 `raise`한다. `print`만 하면 파일이 빠져도 사람이 못 보고 지나쳐서
나중에 엉뚱한 곳에서 터진다.

경로가 여러 개 잡히면 경고한다. zip을 두 번 풀면 실제로 이런 일이 생긴다.

`df["inst"]`로 촬영 기관 코드를 뽑는다. 환자 ID가 `TCGA_<기관>_<번호>_<날짜>` 형식이라
문자열 분리만으로 얻을 수 있다.

**질문받으면**
> Q. 왜 마스크부터 찾나요?
> A. 원본과 마스크가 같은 폴더에 `X.tif` / `X_mask.tif`로 있습니다.
>    `*.tif`로 잡으면 둘 다 걸리는데, 마스크는 이름이 특정되므로
>    `*_mask.tif`로 잡고 이름을 변환하는 쪽이 안전합니다.

## 1-1. 클래스 분포 ★★

**무엇을**
슬라이스 단위(종양 34.9%)와 픽셀 단위(1.03%) 불균형을 각각 계산한다.

**왜**
**이 셀이 프로젝트 전체의 근거다.** 전체 픽셀의 1%만 종양이라는 사실에서
이후 모든 설계가 파생된다.

- 픽셀 정확도 무의미 → Dice와 recall 사용
- BCE 단독은 "전부 배경"으로 수렴 위험 → Dice 항 추가
- 빈 슬라이스 65% → 에폭별 재샘플링

4번(흉부 X-ray)에서 NORMAL/PNEUMONIA 장수를 센 것과 같은 목적이지만,
**분할은 슬라이스 불균형과 픽셀 불균형이 따로 있다**는 점이 다르다.

**질문받으면**
> Q. 불균형이 얼마나 심한 건가요?
> A. 4번 흉부 X-ray는 대략 1:3이었습니다. 여기는 1:99입니다.
>    같은 "불균형"이지만 정도가 완전히 다르고, 그래서 대응 방법도 달라집니다.

## 1-1b. 파일 수준 중복

**무엇을**
빈 마스크 2,556개가 바이트 단위로 동일함을 보인다.

**왜**
클래스 불균형을 파일 시스템 수준에서 보여주는 근거.
`value_counts()` 하나보다 구체적이다.
tif 7,858개 중 내용이 다른 파일은 5,303개뿐이다.

**질문받으면**
> Q. 중복이면 지워도 되나요?
> A. 안 됩니다. 빈 마스크는 "이 슬라이스에 종양이 없다"는 정답 데이터입니다.
>    지우면 모델이 종양 있는 슬라이스만 보고 학습해서 오탐이 폭발합니다.

## 1-2. 기관별 분포 ★

**무엇을**
기관 5개의 환자 수, 슬라이스 수, 종양 비율, FLAIR 평균 밝기를 낸다.

**왜**
```
DU 45명  밝기 0.1905
CS 16명  밝기 0.2445    <- DU보다 28% 밝음
EZ  1명  밝기 0.2506
```
스캐너와 프로토콜이 달라 밝기가 크게 차이 난다.
**이 정보가 2번 셀의 층화 기준이 된다.**

**질문받으면**
> Q. 밝기 차이를 어떻게 처리했나요?
> A. 두 가지입니다. 첫째, 분할 시 기관을 층화 기준에 넣어 세 세트에 고르게 분배했습니다.
>    둘째, 정규화를 train 통계로만 계산했습니다. 추가로 강도 증강을 넣는 것도 검토 중입니다.

## 1-3. 시각화

**무엇을**
종양 있는 슬라이스 3장의 채널 3개 + 마스크 + 오버레이를 그린다.

**왜**
5번의 "눈으로 확인하기" 셀과 같은 방식이지만 열 구성이 다르다.
5번은 컬러 이미지 1장이었는데, **여기 3채널은 RGB가 아니라 서로 다른 촬영 기법**
(pre-contrast / FLAIR / post-contrast)이라 펼쳐서 봐야 한다.

**질문받으면**
> Q. 채널이 RGB가 아니라는 게 무슨 뜻인가요?
> A. MRI를 세 가지 방식으로 촬영한 결과를 채널에 쌓아둔 겁니다.
>    마스크는 FLAIR 이상 소견을 그린 것이라 FLAIR 채널이 가장 중요합니다.
>    참고로 110명 중 15명은 촬영 시퀀스가 결측되어 FLAIR로 대체되어 있습니다.

---

# 2. 데이터 분할 ★★★

## 2-0. split_by_patient

**무엇을**
환자를 기관 × 종양비율 2분위로 층화해 train 78 / val 16 / test 16명으로 나눈다.

**왜 — 이 노트북에서 가장 중요한 셀**

4번에서는 `torch.randperm`으로 인덱스를 섞어 나눴다. 거기서는 맞는 방법이었다.
X-ray 한 장이 환자 한 명이었기 때문이다.

**여기서 그대로 쓰면 안 된다.** 환자 한 명이 슬라이스 평균 36장이다.
슬라이스를 섞어 나누면 같은 환자의 3번이 train에, 4번이 test에 들어간다.
둘은 사실상 같은 그림이라 Dice가 부풀려진다.

층화 기준에 **기관**을 넣은 이유도 있다. 종양 비율만으로 층화한 초기 분할에서는
CS 기관 환자가 test에 한 명도 들어가지 않았다.

EZ 기관은 환자가 1명이라 층화가 불가능해 train에 고정했다. 보고서에 명시할 것.

`assert leak == 0`으로 환자 교집합이 0인지 검증한다.

**질문받으면**
> Q. 왜 슬라이스 단위로 나누면 안 되나요?
> A. 실제로 해봤습니다. test 환자 109명 전원이 train에도 들어갔습니다.
>    환자당 슬라이스가 36장이라 랜덤으로 15%를 떼면 모든 환자가 양쪽에 걸립니다.
>
> Q. 층화 기준을 더 늘리면 안 되나요?
> A. 110명을 이미 기관 5 × 종양비율 2 = 최대 10층으로 나눴습니다.
>    종양 크기까지 넣으면 20층이 되고 층당 5.5명, 15%면 0.8명이라
>    반올림하면 0명인 층이 생깁니다. 대신 층화하지 않은 변수는 사후 검증했습니다.

## 2-1. 잘못된 방식 재현

**무엇을**
슬라이스 단위로 나눴을 때 환자가 얼마나 겹치는지 보인다.

**왜**
"슬라이스 단위는 안 된다"는 주장의 실증. 말로 하는 것보다 숫자가 강하다.

## 2-2. 층화 사후 검증 ★

**무엇을**
층화하지 않은 변수(종양 크기, 환자당 슬라이스)가 우연히 균형 잡혔는지 확인한다.

**왜**
층화는 두 가지로만 했으므로 나머지는 검증해야 한다.
결과: 중앙값은 균형(train 1,487 / val 1,766 / test 1,600px)이지만
test의 75% 분위가 3,482px로 30% 높고, val의 최소 종양이 37px로 가장 크다.

**val에 작은 종양이 적다**는 게 중요하다. 임계값을 val에서 정하는데,
작은 종양이 없으면 임계값이 높게 잡혀 test의 소형 종양을 놓칠 수 있다.
그래서 임계값을 최고점이 아니라 **고원 중앙**으로 잡는다(10번 셀).

**주의:** 1번 셀은 `df`를 새로 만들기 때문에, 1번을 다시 실행하면
`tumor`, `tumor_px`, `split`이 전부 사라진다. 1→1-1→2 순서로 다시 돌려야 한다.

---

# 3. 메모리 적재

## 3-0. build_dataset

**무엇을**
3,929장을 uint8로 RAM에 올린다 (0.96GB, 3.4초).

**왜**
5번의 `build_dataset`을 그대로 가져왔다.
매 에폭 tif를 다시 디코딩하는 것보다 훨씬 빠르다. 에폭 3분을 맞추는 데 기여한다.

`cv2.imread`는 BGR로 읽으므로 `[:, :, ::-1]`로 되돌려
pre-contrast / FLAIR / post-contrast 순서를 맞춘다.

## 3-1. 정규화 통계 ★★

**무엇을**
train 슬라이스에서 채널별 평균과 표준편차를 계산한다.

**왜**
4번에서는 `IMAGENET_MEAN = [0.485, 0.456, 0.406]`을 그대로 썼다.
전이학습이라 사전학습 모델의 통계에 맞춰야 했기 때문에 옳았다.

**여기서는 쓰면 안 된다.** 실측값이 `[0.0858, 0.0822, 0.0891]`로 ImageNet의 5분의 1이다.
MRI는 뇌 바깥이 전부 검은 배경이라 평균이 낮고, 채널도 RGB가 아니다.

**val/test를 포함해 계산하면 데이터 누수다.** train에서만 계산한다.

**float64로 누적**한다. 5억 개 픽셀을 float32로 더하면 반올림이 쌓여
평균이 2% 이상 틀어진다(실측 0.0966 vs 0.0992).

**질문받으면**
> Q. 정규화 통계도 누수가 되나요?
> A. 됩니다. 가이드라인의 "train/test 분리 이후에 스케일링 fit"과 같은 원리입니다.
>    표 데이터의 StandardScaler를 test 포함해서 fit하면 안 되는 것과 똑같습니다.

---

# 4. Dataset / DataLoader ★★

## 4-0. SegDataset

**무엇을**
5번의 `SegDataset` 구조에 빈 슬라이스 재샘플링과 증강을 추가했다.

**왜 — 두 가지가 핵심**

**(1) 마스크 dtype이 바뀐다**
```python
# 5번 (30클래스)
mask = torch.from_numpy(self.masks[i].astype(np.int64))    # (H,W) 클래스 번호
# 이번 (이진)
torch.from_numpy(msk)[None].float()                        # (1,H,W) 0.0/1.0
```
`CrossEntropyLoss`는 클래스 번호를 받지만 `BCEWithLogitsLoss`는 확률을 받는다.

**(2) resample_epoch — 에폭마다 빈 슬라이스를 새로 뽑는다**
train의 빈 슬라이스를 다 넣으면 학습이 희석되고, 다 빼면 오탐이 폭발한다.
매 에폭 다르게 뽑아 섞으면 30에폭 동안 결국 다 보게 되므로
**데이터를 버리는 게 아니라 한 에폭 안의 비율만 조절**하는 셈이다.

val/test는 `neg_ratio=None`이라 재샘플링하지 않고 전체를 쓴다.
평가는 실제 분포에서 해야 의미가 있다.

**(3) 증강을 numpy로 직접 구현**
```python
# 기하 변환: 이미지와 마스크에 동일하게
if rng.random() < 0.5: img, msk = img[:, ::-1], msk[:, ::-1]
```
2번·4번에서 쓴 `transforms.RandomHorizontalFlip`은 **이미지만** 뒤집는다.
마스크는 그대로 남아 정답이 어긋난다. 그래서 직접 짰다.

검증 결과 2,000회 시행에서 이미지-마스크 불일치 0건,
8가지 방향(뒤집기 2 × 2 × 회전 4 중 중복 제거)이 12.1~12.7%로 균등 출현.

**질문받으면**
> Q. albumentations를 쓰면 되지 않나요?
> A. 수업에서 다루지 않은 라이브러리라 제외했습니다.
>    필요한 기하 변환은 numpy 슬라이싱과 `np.rot90`으로 구현 가능합니다.
>
> Q. 빈 슬라이스를 왜 버리지 않나요?
> A. 실제 추론에서는 뇌 꼭대기나 아래쪽 정상 슬라이스가 계속 들어옵니다.
>    학습에서 빼면 모델이 "어딘가에는 반드시 종양이 있다"고 배워
>    정상 슬라이스에 오탐을 냅니다.

---

# 5. U-Net ★★

## 5-0. conv2d_block / EncoderBlock / DecoderBlock / UNet

**무엇을**
5번의 U-Net 구조를 이진 분할용으로 수정했다.

**왜 — 바꾼 곳은 두 군데뿐**

**(1) 출력 채널 30 → 1**
4번에서 `nn.Linear(128, 1)`로 이진 출력을 만든 것과 같은 원리다.
예측도 `argmax(1)` 대신 `sigmoid() > threshold`가 된다.

**(2) BatchNorm 추가 (2번에서 배운 것)**
Cityscapes처럼 클래스가 균형 잡힌 문제에서는 없어도 학습이 됐지만,
종양 픽셀 1%인 여기서는 초반 수렴이 불안정하다.

`use_bn=False`로 두면 5번 원본과 사실상 동일하다.
파라미터 수가 2,160,177 대 2,158,705로 **0.07%만 차이 난다.**
이걸 **베이스라인 M1**으로 쓴다.

`_init_weights`의 `kaiming_normal_`은 2번의 `initializer_weights`와 같다.

**질문받으면**
> Q. skip connection이 왜 필요한가요?
> A. 인코더에서 풀링으로 해상도를 1/16까지 줄이는데, 그 과정에서 위치 정보가 사라집니다.
>    디코더가 복원할 때 인코더의 고해상도 특징을 이어붙여(`torch.cat`) 경계를 살립니다.
>    종양이 7픽셀인 경우도 있어서 이 데이터에서는 특히 중요합니다.
>
> Q. output_padding=1은 왜 있나요?
> A. `ConvTranspose2d`로 2배 업샘플할 때 크기가 정확히 맞지 않으면
>    skip connection과 `torch.cat`이 실패합니다. 실행해서 256이 정확히 복원되는 것을 확인했습니다.

---

# 6. 손실함수 ★★★

## 6-0. BCEDiceLoss / TverskyLoss

**무엇을**
BCE + Dice 조합 손실과, 놓침에 가중치를 주는 Tversky 손실을 정의한다.

**왜**

**5번은 `CrossEntropyLoss`** (30클래스), **4번은 `BCEWithLogitsLoss`** (이진)였다.
여기는 이진이므로 4번 쪽이 맞지만 **BCE 단독은 위험하다.**
종양 픽셀이 1%라 "전부 배경"으로 답해도 손실이 충분히 낮아
경사하강법 입장에서 종양을 찾을 이유가 없다.

**Dice 단독은 초반이 불안정하다.** 예측이 거의 0일 때 분모가 작아 기울기가 튄다.

`smooth=1.0`이 빈 마스크에서 0/0을 막는다.
정답 0, 예측 0이면 `(0+1)/(0+1)=1`이 되어 "잘 맞췄다"로 처리된다.
**빈 슬라이스를 학습에 넣을 수 있는 근거가 이것이다.**

**AMP 주의 — `logits.float()` 한 줄이 중요하다.**
float16으로 65,536픽셀을 더하면 한계값 65,504를 넘어 분모가 inf가 된다.
그러면 Dice 손실이 정확히 1.0으로 고정되고 그래디언트가 사라진다.
**오류도 nan도 뜨지 않아서** loss가 1.0에서 안 움직이는 걸 보며 며칠 헤맬 수 있다.

`BCEWithLogitsLoss`는 파이토치가 자동으로 float32 처리하지만,
직접 짠 Dice/Tversky는 그 목록에 없어서 명시적으로 캐스팅해야 한다.

**질문받으면**
> Q. BCE와 Dice를 왜 섞나요?
> A. BCE는 픽셀 하나하나를 독립적으로 보고, Dice는 겹치는 영역 전체를 봅니다.
>    BCE만 쓰면 소수 클래스를 무시하고, Dice만 쓰면 초반 기울기가 불안정합니다.
>    가중치 0.5씩 더해 둘의 단점을 상쇄합니다.

## 6-1. Tversky 동작 검증 ★

**무엇을**
오차 픽셀 수를 같게 맞춘 두 오답(과소예측/과대예측)에 대해
`beta`가 어느 쪽을 덜 싫어하는지 확인한다.

**왜**
`alpha=1-beta`로 묶으면 `beta`를 올릴 때 `alpha`가 같이 내려가서
**손실값만 봐서는 방향을 알 수 없다.** 그래서 이런 방식으로 검증한다.

결과: `beta > alpha`면 과대예측을 선호(재현율↑), `beta < alpha`면 과소예측 선호(정밀도↑).

**중요:** `beta`가 다르면 손실함수 자체가 다르므로 **손실값끼리 비교하면 안 된다.**
M2와 M3의 학습 곡선을 loss로 비교하면 틀린다. Dice 같은 공통 지표로 비교할 것.

## 6-2. 평가 지표

**무엇을**
`dice_score`, `iou_score`, `pixel_recall`, `volume_ratio`를 정의한다.

**왜**
5번의 `compute_miou`는 다중 클래스용이라 못 쓴다.

**슬라이스마다 따로 내서 평균 내지 않고 교집합과 합을 누적한 뒤 마지막에 나눈다.**
슬라이스별 평균을 쓰면 종양 7픽셀짜리가 8,000픽셀짜리와 같은 무게를 갖는다.

`volume_ratio`(예측 체적 / 정답 체적)가 임상적으로 중요하다.
Dice가 같아도 과대평가와 과소평가는 의미가 정반대다.

## 6-3. 속도 측정

**무엇을**
더미 텐서로 20스텝만 돌려 에폭 시간을 추정한다.

**왜**
본 학습 전에 3분 기준 충족 여부를 확인한다.
실측 결과 `base=32` + `batch=16`이 16.3초, `base=64`도 41.5초로 전부 여유.

`try/except OutOfMemoryError`와 `finally`의 `empty_cache()`가 중요하다.
없으면 OOM 한 번에 이후 측정이 전부 실패한다.

---

# 7. 베이스라인 M0 ★★★

## 7-0. otsu_predict

**무엇을**
FLAIR 강도에 Otsu 임계값을 걸고 가장 큰 덩어리만 남긴다. **학습이 없다.**

**왜**
2번에서 `SimpleCNN` 전에 `SimpleDNN`을 먼저 만들어 기준점을 잡은 것과 같은 구조다.

실측 결과:
```
M0 Otsu        Dice 0.1487  픽셀recall 0.3445  체적 3.64배  슬라이스recall 1.0000  precision 0.3671
전부 배경       Dice 0.0000  픽셀 정확도 0.9884
항상 종양 있음   슬라이스recall 1.0000  precision 0.3441
```

**이 표가 세 가지를 증명한다.**

1. **픽셀 정확도는 무의미하다** — 아무것도 안 해도 0.9884.
   4번에서 `accuracy_score`를 쓴 건 옳았다(불균형 1:3). 여기는 1:99라 다르다.
2. **재현율은 두 가지를 가리킨다** — 슬라이스 재현율 1.0000 vs 픽셀 재현율 0.3445.
   섞어 쓰면 안 된다.
3. **재현율은 목표가 아니라 제약 조건** — 슬라이스 재현율 1.0인데 체적을 3.64배 과대평가한다.
   추적 관찰에 쓸 수 없다.

`cv2.threshold(THRESH_OTSU)`와 `connectedComponentsWithStats`는 수업 밖 함수다.
numpy 백분위 방식(Dice 0.0441)과 비교해 3배 이상 좋아서 채택했고, 그 근거를 기록한다.

**질문받으면**
> Q. 규칙 기반을 왜 넣었나요?
> A. 두 가지입니다. 첫째, 딥러닝의 개선 폭을 정량화합니다(0.1487 → 0.7327, 4.9배).
>    둘째, 정확도와 재현율이라는 지표가 왜 단독으로 무의미한지를 학습 없이 보여줍니다.

---

# 8. 학습 함수 ★★

## 8-0. evaluate_seg / train_unet

**무엇을**
5번의 `train_unet` 뼈대에 세 가지를 추가했다.

**왜**

**(1) 체크포인트 기준을 뒤집었다**
```python
# 5번:  if val_loss < best_val
# 이번: if va_dice > best_dice
```
val_loss나 픽셀 정확도로 저장하면 **"전부 배경" 모델이 최고점으로 남는다.**

**(2) train Dice도 기록**
과적합 격차를 보려면 loss만으로는 부족하다.
M1 결과: train 0.8721 vs val 0.7262, 격차 +0.1459.

**(3) AMP**
`autocast`로 순전파를 float16으로, **손실은 블록 밖에서 float32로** 계산한다.
`GradScaler`가 손실에 큰 수를 곱해 역전파 후 되돌린다.
둘 다 있어야 한다 — 캐스팅만 하면 그래디언트가 언더플로로 0이 되고,
스케일러만 있으면 분모가 오버플로한다.

`evaluate_seg`는 4번의 `evaluation()` 구조를 따랐다.
`model.eval()` → `@torch.no_grad()` → probs/trues 수집.
4번과 다른 점은 `sigmoid` 후 확률 자체를 돌려준다는 것이다
(5번의 `evaluate_seg`는 pixel accuracy를 반환했지만 여기선 무의미).

`sched = CosineAnnealingLR`은 2번에서 배운 스케줄러다.

---

# 9. 모델 학습

## 9-0~2. M1 / M2 / M3

| 모델 | 구성 | 의미 |
|---|---|---|
| M1 | BCE만, BatchNorm 없음 | 5번 노트북 구조 = 베이스라인 |
| M2 | + BatchNorm + BCE·Dice | 2번에서 배운 BN의 효과 |
| M3 | + Tversky(β=0.7) | 재현율 강화 (임상 전제) |

**M1 실측 결과**
```
최고 val Dice 0.7327 @ 24에폭 / 과적합 격차 +0.1459 / 에폭 평균 20.8초
1에폭에서 이미 val Dice 0.5418
```

**1에폭에서 0.54가 나온 것이 중요한 발견이다.**
"BCE 단독은 전부 배경으로 수렴한다"는 우려가 현실화되지 않았는데,
**빈 슬라이스 재샘플링(`NEG_RATIO=1.0`)이 이미 불균형을 완화했기 때문이다.**
학습 데이터의 절반이 종양을 포함하고 있어 원본 분포(65% 빈 슬라이스)와 다르다.

즉 **불균형 대응 세 층위(샘플링 / 손실함수 / 임계값) 중 첫 번째만으로도
BCE 단독 학습이 성립했다.** 이건 보고서에 쓸 만한 결과다.

**질문받으면**
> Q. M1이 베이스라인인데 왜 성능이 잘 나오나요?
> A. 데이터 구성 단계에서 이미 불균형을 완화했기 때문입니다.
>    손실함수를 바꾸기 전에 샘플링만으로 상당 부분 해결된다는 것을 보여줍니다.

## 9-3. 학습 곡선

**무엇을**
세 모델의 val Dice, 과적합 격차, 에폭 시간을 나란히 그린다.

**왜**
가운데 그림(과적합 격차)이 가이드라인의 "train/test 성능 격차 확인" 항목을 채운다.
오른쪽 그림은 3분 기준 충족 여부를 시각화한다.

---

# 10. 임계값 결정 ★★ (미실행)

**무엇을**
0.05~0.95를 훑어 Dice가 최대인 구간을 찾고, **고원의 중앙값**을 택한다.

**왜**
4번에서는 `sigmoid` 출력을 0.5로 잘랐다. **0.5는 관습일 뿐 근거가 없다.**

**임계값만 바꿔도 지표가 크게 움직이지만, 이건 모델이 좋아진 게 아니다.**
정밀도-재현율 곡선 위에서 서 있는 위치를 옮긴 것뿐이고,
임계값과 무관한 PR-AUC는 전혀 변하지 않는다.

따라서 **모델 비교는 각 모델의 val 최적 임계값에서** 해야 공정하다.
한 모델은 0.5 고정, 다른 모델은 튜닝해서 비교하면 조작이다.

**최고점이 아니라 고원 중앙을 잡는 이유:** val 환자가 16명뿐이라
뾰족한 최고점을 잡으면 노이즈를 잡는 것이다.
게다가 2-2에서 확인했듯 val에는 소형 종양이 적어 임계값이 높게 잡히는 경향이 있다.

**체적비 곡선을 함께 그린다.** 재현율을 올리려고 임계값을 내리면
예측 체적이 정답의 몇 배로 커진다.

---

# 11. 평가 ★★ (미실행)

**무엇을**
픽셀 / 슬라이스 / 환자 세 층위로 평가한다.

**왜**

**픽셀 수준** — Dice, IoU, 체적비.
전체 Dice와 종양 슬라이스만의 Dice를 따로 낸다. 차이가 크면 빈 슬라이스 오탐이 있다는 뜻.

**슬라이스 수준** — 4번의 지표를 그대로 쓴다.
`recall_score`, `precision_score`, `f1_score`, `roc_auc_score`,
`average_precision_score`(PR-AUC), `confusion_matrix`.
**`accuracy_score`만 뺐다** (7번에서 무의미함을 확인).

**환자 수준** — 환자별 Dice + 놓친 슬라이스 번호.
임상 전제의 핵심 산출물이다. 평균 Dice 0.83은 의사에게 아무 지시도 주지 못하지만,
"18번 슬라이스를 다시 보라"는 실행 가능한 정보다.

혼동행렬은 2번의 seaborn 히트맵 방식.
기관별 분해는 "우리 병원 스캐너에서도 되는가"에 답하기 위한 것.

---

# 13. 가설검정 ★★ (미실행)

**무엇을**
M2와 M3의 환자별 Dice를 대응표본 정확 순열검정으로 비교한다.

**왜**

**검정 단위는 환자(n=16)이지 슬라이스(n=622)가 아니다.**
같은 환자의 인접 슬라이스는 거의 같은 그림이라 독립 표본이 아니다.
슬라이스로 검정하면 아무 차이가 없어도 p < 0.001이 나온다.

같은 환자를 두 모델이 각각 평가하므로 **대응표본**이다.
대응 설계를 쓰면 환자마다 다른 난이도가 상쇄되어 표본이 적어도 검정력이 확보된다.

Dice는 0~1에 갇혀 있고 치우쳐 있어 t검정 가정을 만족하지 않는다.
`scipy`는 수업 범위 밖이라 **numpy만으로 정확 순열검정**을 구현했다.
n=16이면 부호 조합이 2^16 = 65,536가지라 **근사가 아닌 전수 계산**이 가능하다.

**해석 시 주의**
1. n이 작으면 검정력이 낮다. `p > 0.05`는 "차이 없음"이 아니라 "차이를 확인하지 못함".
2. p값만 쓰지 말고 중앙값 차이, 효과크기, 95% 신뢰구간을 함께 보고한다.
3. **test는 최종 1회만 개봉.** 결과를 보고 설정을 바꾸면 선택 편향이 들어간다.

**질문받으면**
> Q. 왜 t검정을 안 쓰나요?
> A. Dice는 0과 1 사이에 갇혀 있고 분포가 치우쳐 있어 정규성 가정이 깨집니다.
>    순열검정은 분포 가정이 필요 없고, 표본이 작을 때는 전수 계산이 가능해
>    근사가 아닌 정확한 p값을 얻을 수 있습니다.

---

# 발표 시 강조할 5가지

1. **환자 단위 분할** — 슬라이스 단위로 나누면 test 환자 109명 전원이 train에 들어간다
2. **정확도를 지표에서 뺀 근거** — 전부 배경 예측의 픽셀 정확도가 0.9884
3. **재현율은 목표가 아니라 제약 조건** — M0가 재현율 1.0인데 체적을 3.64배 과대평가
4. **임계값 조정은 모델 개선이 아니다** — PR 곡선 위 위치 이동일 뿐, PR-AUC는 불변
5. **가설검정 단위는 환자** — 슬라이스로 하면 독립성 위반으로 p값이 과소평가된다
