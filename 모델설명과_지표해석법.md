# 모델별 설명과 지표 해석법

`6_lgg_segmentation.ipynb` 기준.
1부 모델 4개가 각각 무엇인가 / 2부 지표를 어떻게 읽는가 / 3부 실제 판단 절차

---

# 1부. 모델 4개

## 전체 구도

네 모델은 **하나씩 바꿔가며 만든 계단**이다. 성능 순위를 매기려고 만든 게 아니라,
**무엇이 성능을 만드는지 분리해서 보려고** 만들었다.

```
M0  학습 없음, 규칙만                    기준점
M1  U-Net + BCE                        ← M0 대비: 딥러닝의 효과
M2  M1 + BatchNorm + Dice 손실          ← M1 대비: 정규화·손실의 효과
M3  M2에서 손실만 Tversky               ← M2 대비: 재현율 가중의 효과
```

**한 번에 하나씩만 바뀐다.** 그래야 차이의 원인을 특정할 수 있다.

---

## M0 — 규칙 기반 (학습 없음)

### 무엇을 하나

FLAIR 채널에서 밝은 곳을 찾아 가장 큰 덩어리 하나만 남긴다.

```python
t, _ = cv2.threshold(flair[brain], 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
pred = (flair > t) & brain
# 연결된 덩어리 중 가장 넓은 것만
```

**Otsu**는 밝기 히스토그램을 보고 "이 값에서 자르면 두 무리로 가장 잘 나뉜다"는 지점을
자동으로 찾는 방법이다. 사람이 임계값을 정하지 않는다.

**연결성분**은 붙어 있는 픽셀 덩어리를 찾아 번호를 매긴다.
Otsu만 쓰면 두개골 경계·혈관·노이즈가 흩어져 나오므로, 가장 큰 덩어리만 남긴다.

### 왜 만들었나

2번 노트북에서 `SimpleCNN` 전에 `SimpleDNN`을 먼저 만들어 기준점을 잡은 것과 같은 구조다.

**딥러닝의 개선 폭을 정량화**하고, **지표가 왜 함정인지 실증**하기 위한 것이다.

### 결과

```
픽셀 Dice        0.1487 -> 경계를 맞게 그렸는지?
픽셀 재현율      0.3445  -> 종양 픽셀중 몇개를 실제로 칠햇는지?
체적비           3.64배 -> 크기를 맞게 잿는지?
슬라이스 recall  1.0000 -> 종양이 있는 컷을 놓쳣는가?
슬라이스 precision 0.3671 -> 종양사진을 얼마나 찾앗는지?

[참고] 전부 배경 예측 시 픽셀 정확도 0.9884
```

### 이 표가 증명하는 것

**① 정확도는 쓸 수 없다.** 아무것도 예측하지 않아도 0.9884가 나온다.
4번(흉부 X-ray)에서 `accuracy_score`를 쓴 것은 타당했다. 거기는 불균형이 약 1:3이었다.
여기는 1:99다.

**② 재현율 단독도 쓸 수 없다.** M0의 슬라이스 재현율이 1.0000인데,
"무조건 종양 있음"이라 답한 것(0.3441)과 정밀도가 거의 같다(0.3671).

**③ 체적비가 그 근거다.** 픽셀 재현율은 0.3445에 불과한데 예측 면적은 3.64배다.
종양 픽셀의 3분의 1만 맞히면서 크기를 3.6배로 그렸다.
저등급 신경교종은 추적 관찰하는 경우가 많아, 크기를 3.6배로 재는 도구는 쓸 수 없다.

> **결론: 재현율은 목표가 아니라 제약 조건이어야 한다.**

---

## M1 — U-Net 베이스라인

### 무엇을 하나

5번 노트북(Cityscapes)의 U-Net을 이진 분할로 바꾼 것.
**BatchNorm 없음, BCE 손실 단독.**

```python
model = UNet(base=32, use_bn=False)
criterion = nn.BCEWithLogitsLoss()
```

### 5번과 무엇이 다른가

|           | 5번              | M1                          |
| --------- | ---------------- | --------------------------- |
| 출력 채널 | 30               | **1**                       |
| 손실      | CrossEntropyLoss | **BCEWithLogitsLoss**       |
| 마스크    | int64 `(B,H,W)`  | **float32 `(B,1,H,W)`**     |
| 예측      | `argmax(1)`      | **`sigmoid() > threshold`** |

**구조는 완전히 동일하다.** 파라미터 8,630,497개로 BatchNorm 버전(8,633,441)과 0.03% 차이.

### U-Net이 왜 필요한가

분할에는 딜레마가 있다. CNN은 층을 지날수록 해상도를 줄인다.

```
256 → 128 → 64 → 32 → 16
```

넓은 범위를 보려면 줄여야 하는데, 출력은 다시 256×256이어야 한다.
16×16에서 되돌리면 위치가 뭉개진다.

**skip connection**이 해법이다. 줄이기 전의 고해상도 정보를 디코더로 직접 전달한다.

```
인코더 ──── skip ────> 디코더
 256                    256
 128                    128
  64                     64
  32                     32
  16 ──> bottleneck ──>  16
```

**이 데이터의 최소 종양이 7픽셀**이므로 skip connection 없이는 복원이 불가능하다.

### BCE 단독의 위험

BCE는 픽셀을 하나씩 독립적으로 채점한다.

```
정답 1일 때  손실 = -log(예측확률)
정답 0일 때  손실 = -log(1 - 예측확률)
```

종양 픽셀이 1.03%이므로, **"전부 배경"이라 답해도 손실이 이미 낮다.**

```
배경 64,860개 × -log(0.99) + 종양 676개 × -log(0.01)
= (64,860 × 0.010 + 676 × 4.605) / 65,536 ≈ 0.057
```

경사하강법 입장에서 종양을 찾을 이유가 약하다. **그 상태에 갇힐 수 있다.**

### 실제로는 안 갇혔다

```
[M1]  1/30  loss 0.0690  train_dice 0.3442  val_dice 0.5418
```

**1에폭에 val Dice 0.5418.** 예상과 달랐다.

원인은 **빈 슬라이스 에폭별 재샘플링**이다. `NEG_RATIO=1.0`이 불균형을 65%에서 50%로
완화해 두었으므로, "전부 배경" 함정이 약해진 것이다.

> **이것이 이 프로젝트의 핵심 발견이다.**
> 불균형 대응을 데이터·손실·임계값 세 층위로 설계했는데, **첫 층위만으로 충분했다.**

### 결과

```
최고 val Dice 0.7327 @ 24에폭
최종 train Dice 0.8721   과적합 격차 +0.1459
test Dice 0.6684  recall 0.9346  PR-AUC 0.8376  체적비 0.99배
```

**약점은 과적합**이다. 17에폭 이후 val이 멈췄는데 train만 계속 올랐다.

---

## M2 — BatchNorm + BCE·Dice

### 무엇을 바꿨나

**두 가지를 동시에** 바꿨다.

```python
model = UNet(base=32, use_bn=True)      # ① BatchNorm 추가 [2번 노트북]
criterion = BCEDiceLoss()                # ② Dice 손실 혼합
```

### BatchNorm이 하는 일

각 층의 출력을 평균 0, 분산 1로 정규화한다. 2번 노트북에서 배운 것.

**학습을 안정시키고 약한 정규화 효과**가 있다. M1의 과적합 격차 +0.1459를 줄이려는 목적.

### Dice 손실을 섞는 이유

```
        2 × 겹친 픽셀
Dice = ─────────────────
       예측 + 정답
```

**Dice는 배경을 세지 않는다.** 분자도 분모도 종양 영역만 본다.
"전부 배경"이면 Dice가 0이므로, 종양을 찾아야만 손실이 줄어든다.

```python
return 0.5 * bce + 0.5 * dice
```

BCE는 안정적이지만 불균형에 약하고, Dice는 불균형에 강하지만 초반 기울기가 불안정하다.
**서로의 단점을 상쇄한다.**

### smooth=1.0

빈 마스크에서 `0/0`을 막는다.

```
정답 0, 예측 0  →  (2×0 + 1) / (0 + 1) = 1.0  →  dice_loss = 0
```

**빈 슬라이스를 학습에 넣을 수 있는 근거가 이것이다.**
이 항이 없으면 nan이 발생하고, nan은 그래디언트를 타고 모델 전체로 퍼져 복구 불가능하다.

### AMP 함정

```
float16 최댓값     65,504
256 × 256 픽셀     65,536   ← 이미 초과
```

`p.sum()`이 이 한계를 넘으면 분모가 inf가 되어 **Dice 손실이 정확히 1.0으로 고정**된다.
오류도 nan도 발생하지 않고 조용히 학습이 멈춘다.

```python
logits, target = logits.float(), target.float()   # 필수
```

`BCEWithLogitsLoss`는 파이토치가 자동으로 float32 처리하지만,
**직접 구현한 Dice·Tversky는 그 목록에 없다.**

### 결과

```
최고 val Dice 0.7155   과적합 격차 +0.0776   ← M1의 절반
test Dice 0.6568  recall 0.6822  PR-AUC 0.7735  체적비 0.66배
```

**과적합은 줄었으나 검출력이 크게 떨어졌다.** recall 0.9346 → 0.6822.

### 왜 나빠졌나

**재샘플링이 이미 불균형을 해결했기 때문**이다.
Dice 항의 존재 이유였던 문제가 사라진 상태에서 손실을 섞으니 학습만 불안정해졌다.

```
[M2]  5/30  val_dice 0.6998
[M2]  6/30  val_dice 0.6477
[M2]  7/30  val_dice 0.4557   ← 급락
[M2]  8/30  val_dice 0.6574
```

7에폭에 loss가 0.31→0.21로 급락하면서 train Dice는 0.73→0.64로 **떨어졌다.**
손실은 줄었는데 성능이 나빠진 것으로, BCE와 Dice의 주도권이 바뀌는 지점으로 보인다.

---

## M3 — Tversky (재현율 강화)

### 무엇을 바꿨나

M2에서 **손실만** 교체.

```python
criterion = TverskyLoss(alpha=0.3, beta=0.7)
```

### Tversky가 무엇인가

```
              TP
T = ──────────────────────
    TP + α·FP + β·FN

TP  맞게 찾은 종양 픽셀
FP  아닌데 종양이라 한 것 (오탐)
FN  종양인데 놓친 것       (놓침)
```

**β를 키우면 FN(놓침)의 벌점이 커진다.** 모델이 놓치지 않으려고 마스크를 넓게 그린다.

```
α = β = 0.5    Dice와 동일
α=0.3, β=0.7   놓침을 더 싫어함 → 재현율 상승
α=0.7, β=0.3   오탐을 더 싫어함 → 정밀도 상승
```

### 왜 도입했나

**임상 전제(놓친 종양 최소화)를 손실함수에 반영**하기 위해서다.
평가만 임상적으로 하고 학습은 그대로면 앞뒤가 맞지 않는다.

### 검증 방법 — 손실값을 비교하면 안 된다

`α=1-β`로 묶으면 β를 올릴 때 α가 함께 내려가 두 효과가 섞인다.
**손실값만 봐서는 방향을 알 수 없다.**

**오차 픽셀 수를 동일하게 맞춘 두 오답**을 비교해야 한다.

| α   | β   | 과소예측(놓침 32) | 과대예측(오탐 32) | 선호         |
| --- | --- | ----------------- | ----------------- | ------------ |
| 0.5 | 0.5 | 0.3322            | 0.2020            | 과대예측     |
| 0.3 | 0.7 | 0.4070            | 0.1326            | 과대예측     |
| 0.1 | 0.9 | 0.4667            | 0.0500            | 과대예측     |
| 0.7 | 0.3 | 0.2358            | 0.2611            | **과소예측** |

마지막 행이 대조군이다. **α와 β의 역할이 반대**임을 보여준다.

부수적 발견: **α=β=0.5(Dice)일 때도 과대예측을 선호**한다(0.2020 < 0.3322).
놓치면 TP까지 함께 줄기 때문이며, **Dice 자체에 약한 재현율 편향**이 있다.

### 결과 — 의도와 반대

```
최고 val Dice 0.6664   과적합 격차 +0.0083
test Dice 0.6438  recall 0.6822  precision 0.8902  체적비 0.77배
```

**재현율이 오르지 않고 오히려 정밀도 쪽으로 기울었다.**

### 왜 그런가

**학습이 완료되지 않았다.**

```
20에폭  train 0.6311
28에폭  train 0.6635
30에폭  train 0.6703   ← 계속 상승 중
과적합 격차 +0.0083     ← 일반화가 좋은 게 아니라 train조차 못 맞힘
```

Tversky는 BCE 항이 없어 수렴이 느리다.
학습이 덜 된 모델은 확신이 없어 조심스럽게 예측하고, 그 결과 검출을 덜 한다.

**"β를 키우면 놓침에 벌점"이라는 설계는 모델이 어느 정도 학습된 뒤에야 발현된다.**

> 따라서 M3의 낮은 성능이 **Tversky 손실의 한계인지 학습 부족인지 본 실험으로는 구분할 수 없다.**
> 동일 에폭 조건 유지를 위해 추가 학습은 수행하지 않았다.

---

## 모델 비교 요약

|                 | M0     | M1         | M2         | M3          |
| --------------- | ------ | ---------- | ---------- | ----------- |
| 학습            | 없음   | BCE        | BCE+Dice   | Tversky     |
| BatchNorm       | —      | 없음       | 있음       | 있음        |
| test Dice       | 0.1487 | **0.6684** | 0.6568     | 0.6438      |
| 슬라이스 recall | 1.0000 | **0.9346** | 0.6822     | 0.6822      |
| precision       | 0.3671 | 0.8065     | **0.9299** | 0.8902      |
| PR-AUC          | —      | **0.8376** | 0.7735     | 0.7296      |
| 체적비          | 3.64배 | **0.99배** | 0.66배     | 0.77배      |
| 과적합 격차     | —      | +0.1459    | +0.0776    | **+0.0083** |
| 놓친 환자       | —      | **8/16**   | 15/16      | 13/16       |

**최종 선택: M1.** PR-AUC가 가장 높고, 임상 최우선 지표인 슬라이스 recall에서 압도적이며,
체적비가 1.0에 가장 가깝다.

---

# 2부. 지표 해석법

## 지표는 세 층으로 나뉜다

```
1층  PR-AUC              "어느 모델이 나은가"        → 모델 선택
2층  운영점 4개           "실제로 어떤 성능인가"      → 균형 확인
3층  최악 시나리오        "가장 나쁠 때 얼마나 나쁜가" → 사용 가능 판단
```

**층마다 판단 기준이 다르다.** 아래로 갈수록 구체적이고 실제적이다.

---

## 1층 — PR-AUC

### 무엇인가

**임계값을 0부터 1까지 전부 훑었을 때의 정밀도-재현율 곡선 아래 면적.**

임계값을 어떻게 정하든 값이 변하지 않는다.
따라서 **모델 자체의 품질**을 나타낸다.

### 왜 이것으로 비교하나

임계값을 바꾸면 Dice·recall·precision이 전부 움직인다.
**한 모델은 0.5 고정, 다른 모델은 튜닝해서 비교하면 조작**이다.

PR-AUC는 그 문제가 없다.

```
M1  0.8376
M2  0.7735
M3  0.7296
```

**M1의 우위는 임계값 선택의 결과가 아니라 모델 자체의 차이다.**

### 왜 ROC-AUC가 아닌가

ROC-AUC는 배경(음성 클래스)이 압도적으로 많을 때 낙관적으로 나온다.
음성이 6만 개고 양성이 676개면, 음성을 조금만 맞혀도 곡선이 좋아진다.

**PR-AUC는 양성 클래스에 집중**하므로 불균형 데이터에서 더 정직하다.

### 읽는 기준

```
차이 0.05 이상    의미 있는 격차
차이 0.05 미만    우연일 수 있음, 다른 지표로 판단
```

---

## 2층 — 운영점 4개

**하나씩 보면 반드시 속는다. 네 개를 한 세트로 읽는다.**

### 슬라이스 recall (검출 재현율)

> "종양이 있는 슬라이스를 찾았는가"

**임상적으로 가장 중요하다.** 놓친 종양은 되돌릴 수 없다.

```python
pred_flag = ((probs > threshold).sum(axis=(1,2)) > MIN_PX).astype(int)
recall_score(true_flag, pred_flag)
```

`MIN_PX=20`은 노이즈성 몇 픽셀을 거르는 기준.
**이 데이터의 최소 종양이 7픽셀**이므로, 정확히 7픽셀을 예측하면 기준에 미달해 "없음"이 된다.
recall이 이상하게 낮으면 이 값을 5~10으로 낮춰볼 것.

### 슬라이스 precision

> "종양이라 표시한 것 중 진짜는 얼마인가"

낮으면 **오탐이 많다.** 판독의가 가짜 표시를 계속 확인해야 하면 도구를 쓰지 않는다.

### 픽셀 Dice

> "경계를 얼마나 정확히 그렸는가"

recall과 precision의 결과물이다. **따로 보는 게 아니라 종합 확인용.**

### 체적비

> "크기를 제대로 쟀는가"

```python
volume_ratio = 예측 픽셀 수 / 정답 픽셀 수
1.0이 정확, 2.0이면 2배 과대평가
```

**독립적인 경고등이다.** Dice가 좋아도 체적비가 3배면 쓸 수 없다.

저등급 신경교종은 즉시 수술하지 않고 추적 관찰하므로,
크기를 잘못 재면 "커졌다/줄었다"는 오판을 부른다.

### 묶어 읽는 규칙

```
recall과 precision은 서로 교환 관계
  임계값 ↓ → recall ↑, precision ↓
  둘의 합이 오르면 진짜 개선

체적비는 별도 판정
  1.5배 초과 또는 0.7배 미만이면 임상 부적합
```

### 함정 사례

```
M0    recall 1.0000  precision 0.3671  Dice 0.1487  체적비 3.64배
      → recall만 보면 완벽, 나머지 전부 실패

M2    recall 0.6822  precision 0.9299  Dice 0.6568  체적비 0.66배
      → precision만 보면 M1보다 낫지만 3분의 1을 놓침
```

---

## 3층 — 최악 시나리오

**평균은 거짓말을 한다. 실제 판단은 여기서 일어난다.**

### 평균과 중앙값의 괴리

```
전체 Dice     0.6684
환자 평균      0.6875
환자 중앙값    0.7851   ← 평균보다 0.1 높음
```

**중앙값이 평균보다 높으면 몇몇 환자가 크게 나빠서 평균을 끌어내린 것이다.**

정확한 서술: "대부분의 환자에서 0.78 수준이나 일부에서 실패한다."
"평균 0.69"라고만 쓰면 실상이 보이지 않는다.

### 환자별 최소 Dice

```
M1  0.0773
M2  0.0000   ← 한 환자를 완전히 놓침
M3  0.0000
```

**의사에게 "이상 없음"이라고 잘못 보고하는 경우가 있는가**를 나타낸다.

### 놓친 환자 수

```
M1   8 / 16 (50%)
M2  15 / 16 (94%)
M3  13 / 16 (81%)
```

**recall 0.9346이 균등하게 퍼진 게 아니다.** 절반은 완벽하고 절반은 한두 장씩 놓친다.

### 체적비 최악값

```
M1 최악 과대예측  21.6배
```

한 환자에게서 뇌간 전체를 종양으로 표시했다.
**평균 성능이 아니라 이 사례로 도구의 신뢰가 결정된다.**

### 과적합 격차

```
train Dice - val Dice
M1  +0.1459   학습 데이터를 외우기 시작
M2  +0.0776
M3  +0.0083   과적합이 없는 게 아니라 학습 부족
```

**격차가 작다고 무조건 좋은 게 아니다.** train Dice 자체가 낮으면 미학습이다.

---

## 검출과 경계는 다른 문제다

**가장 중요한 구분.**

```
TCGA_CS_5397
  종양 슬라이스 6장 중 6장 검출     검출율 100%
  Dice 0.0773                     위치는 거의 전부 틀림
  체적 +2,063.5%                  20배로 예측
```

**놓친 것은 하나도 없는데 뇌간과 소뇌 전체를 종양으로 칠했다.**

```
슬라이스 recall  "이 컷에 종양이 있나"      검출
픽셀 Dice        "종양 영역을 맞게 그렸나"   경계
```

**두 재현율을 섞어 쓰면 안 된다.**
하나의 임계값으로 둘을 동시에 만족시킬 수 없다.

---

# 3부. 실제 판단 절차

## 새 결과를 받았을 때

```
1  PR-AUC 를 본다              모델이 나아졌는가
2  recall + precision 합       운영점이 나아졌는가
3  체적비                      1.0에서 멀어지지 않았는가
4  놓친 환자 수                실패가 덜 극단적인가
5  환자 최소 Dice              최악이 개선됐는가
```

## 판정 조합

| 1층  | 3층  | 판정                                      |
| ---- | ---- | ----------------------------------------- |
| 상승 | 개선 | 확실한 개선                               |
| 유지 | 개선 | 안정성 개선 (충분히 의미 있음)            |
| 상승 | 악화 | 평균만 오르고 최악이 나빠짐 → 임상 부적합 |
| 하락 | —    | 실패                                      |

## M1의 현재 기준값 (test 16명)

```
[1층]  PR-AUC 0.8376

[2층]  슬라이스 recall 0.9346   precision 0.8065
       픽셀 Dice 0.6684        체적비 0.99배

[3층]  놓친 환자 8/16
       환자 Dice 최소 0.0773   중앙 0.7851
       체적비 최악 21.6배
       과적합 격차 +0.1459
```

## 교차검증 결과가 나오면

**폴드 간 표준편차를 먼저 본다.**

```
0.02 이하    안정적. 단일 분할 결과를 그대로 써도 됨
0.02~0.05    보통. 범위를 함께 표기
0.05 이상    불안정. 단일 값으로 보고하면 안 됨
```

**그다음 환자 110명 분포를 본다.**

```
Dice 0.3 미만 몇 명 (몇 %)
```

지금까지 "16명 중 1명"이라 발생률 신뢰구간이 0.2~30%로 무의미했다.
110명이면 훨씬 좁아진다.

**마지막으로 기관별을 본다.**

단일 분할에서는 CS 2명, FG 2명, EZ 0명이라 판단이 불가했다.
교차검증 후 CS 16명, FG 14명, EZ 1명 전원이 평가된다.
**"기관 간 성능 차이가 실재하는가"에 처음으로 답할 수 있다.**

---

## 종합 판단 문장 예시

> M1은 PR-AUC 0.8376으로 세 모델 중 가장 우수하며, 이는 임계값 선택과 무관한 차이다.
> 운영점에서 슬라이스 재현율 0.9346, 체적비 0.99배로 검출과 크기 추정 모두
> 임상 요구를 충족한다.
>
> 다만 성능이 환자 간에 고르지 않다. 16명 중 8명에서 일부 슬라이스를 놓쳤고,
> 최저 환자는 Dice 0.0773으로 종양 인접 영역까지 과대 분류하여
> 체적을 21.6배로 예측하였다.
> 환자별 Dice 중앙값 0.7851과 평균 0.6875의 괴리가 이 편차를 보여준다.
>
> 따라서 M1은 집단 수준 지표로는 사용 가능하나,
> 개별 환자 판독 보조로는 추가 검증이 필요하다.

**"좋다/나쁘다"가 아니라 "어디까지 쓸 수 있다"로 결론이 나오는 것이 정상이다.**
