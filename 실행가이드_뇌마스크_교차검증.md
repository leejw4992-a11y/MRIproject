# 실행 가이드 — 뇌 마스크 대책 + 교차검증

`6_lgg_segmentation.ipynb` 기준. 셀 번호는 현재 노트북의 인덱스.

---

# 0. 전체 그림

```
[준비]  기존 정의 셀 복구            1분 20초
[진단]  뇌 바깥 오탐 측정            1분      ← 학습 불필요, 여기서 방향 결정
[정의]  새 셀 3개 추가               3초
[실행]  교차검증 A / B               50분
[분석]  결과 비교                    1분
```

**핵심: [진단] 결과에 따라 [실행] 설계가 바뀐다.** 먼저 돌릴 것.

---

# 1. 준비 — 기존 셀 복구

커널이 비어 있으므로 **정의 셀만** 순서대로 실행한다. 학습 셀은 건너뛴다.

| 순서 | 셀 | 내용 | 소요 |
|---|---|---|---|
| 1 | `[2]` | import + device | 1초 |
| 2 | `[4]` | 하이퍼파라미터 ★ 한 번도 실행 안 됨 | 1초 |
| 3 | `[8]` | 경로 + 인덱싱 | 3초 |
| 4 | `[10]` | 종양 유무 | 10초 |
| 5 | `[14]` | 기관별 | 20초 |
| 6 | `[18]` | 분할 | 1초 |
| 7 | `[21]` | 메모리 적재 | 10초 |
| 8 | `[25]` | 정규화 통계 | 30초 |
| 9 | `[27]` | SegDataset | 1초 |
| 10 | `[29]` | U-Net 블록 | 1초 |
| 11 | `[31]` | 손실함수 | 1초 |
| 12 | `[35]` | 평가 지표 | 1초 |
| 13 | `[42]` | evaluate_seg + train_unet | 1초 |
| 14 | `[54]` | threshold_sweep + pick_threshold | 1초 |
| 15 | `[57]` | MIN_PX + evaluate_full | 1초 |

**건너뛸 셀**: `[16] [23] [45] [48] [50] [52] [55] [61] [67] [70]`
(시각화, 학습 — 총 40분 절약)

### 기존 M1 불러오기

`[57]` 다음에 새 셀로 추가.

```python
# 기존 학습 결과 복구 (재학습 불필요)
model_1 = UNet(base=BASE, use_bn=False).to(device)
model_1.load_state_dict(torch.load('lgg_M1.pt', map_location=device))
model_1.eval()

_, _, val_loader, test_loader = make_loaders()
vp, vt = evaluate_seg(model_1, val_loader)
BEST_T = {'M1': pick_threshold(threshold_sweep(vp, vt))}
print(f"M1 복구 완료. 임계값 {BEST_T['M1']:.2f}")
```

### 확인

```python
need = ['df','data','MEAN','STD','UNet','evaluate_seg','dice_score',
        'pixel_recall','volume_ratio','threshold_sweep','pick_threshold',
        'MIN_PX','evaluate_full','build_dataset','train_unet','model_1','BEST_T',
        'BASE','BATCH_SIZE','EPOCHS','LR','NEG_RATIO','USE_AMP',
        'NUM_WORKERS','PIN_MEMORY','SEED','IMAGE_SIZE']
missing = [n for n in need if n not in globals()]
print("없는 변수:", missing if missing else "없음 — 진행 가능")
```

---

# 2. 진단 — 뇌 바깥 오탐이 실제로 있는가

**학습 불필요. 여기 결과로 이후 설계가 갈린다.**

## 셀 D-1. 뇌 마스크 함수

```python
def brain_mask(img_float, thr=0.05):
    """FLAIR 채널 기준 뇌 영역. img_float 는 0~1 범위 (H,W,3)"""
    return img_float[:, :, 1] > thr


def apply_brain_constraint(probs, imgs_u8, thr=0.05):
    """뇌 바깥 예측을 0으로"""
    out = probs.copy()
    for i in range(len(probs)):
        bm = brain_mask(imgs_u8[i].astype(np.float32) / 255.0, thr)
        out[i][~bm] = 0.0
    return out


# 뇌 비율 확인 (25~40% 가 정상)
tr_imgs = data['train'][0]
ratios = [brain_mask(tr_imgs[i].astype(np.float32)/255.).mean()
          for i in range(0, len(tr_imgs), 50)]
print(f"뇌 영역 비율  평균 {np.mean(ratios):.3f}  "
      f"최소 {np.min(ratios):.3f}  최대 {np.max(ratios):.3f}")
```

## 셀 D-2. 뇌 바깥 오탐 측정

```python
te_imgs, te_msks, te_sub = data['test']
probs, trues = evaluate_seg(model_1, test_loader)
t = BEST_T['M1']

bm_all = np.stack([brain_mask(te_imgs[i].astype(np.float32)/255.)
                   for i in range(len(te_imgs))])
pred = probs > t

fp_out   = int((pred & ~bm_all).sum())
fp_in    = int((pred & bm_all & (trues <= 0.5)).sum())
pred_all = int(pred.sum())

print("=== 예측 픽셀의 위치 분해 ===")
print(f"  전체 예측       {pred_all:,}")
print(f"  뇌 바깥 (배경)   {fp_out:,}  ({fp_out/max(pred_all,1)*100:5.2f}%)")
print(f"  뇌 안 오탐      {fp_in:,}  ({fp_in/max(pred_all,1)*100:5.2f}%)")
print()

probs_c = apply_brain_constraint(probs, te_imgs)
print(f"{'':16s} {'Dice':>8} {'픽셀recall':>10} {'체적비':>8}")
print(f"{'제한 없음':16s} {dice_score(probs,te_msks,t):>8.4f} "
      f"{pixel_recall(probs,te_msks,t):>10.4f} {volume_ratio(probs,te_msks,t):>7.2f}")
print(f"{'뇌 마스크 적용':16s} {dice_score(probs_c,te_msks,t):>8.4f} "
      f"{pixel_recall(probs_c,te_msks,t):>10.4f} {volume_ratio(probs_c,te_msks,t):>7.2f}")
```

## 판정

| 결과 | 해석 | 다음 |
|---|---|---|
| 뇌 바깥 5% 이상 | 배경 오탐 실재 | `brain_constraint=True` 필수 |
| 뇌 바깥 1% 미만 | 배경 문제 아님 | `brain_constraint` 생략 가능, 실험 단순화 |
| Dice가 오름 | 후처리가 이득 | 최종 파이프라인에 포함 |
| Dice 변화 없음 | 이미 뇌 안에만 예측 | 문제는 뇌 **안쪽** 오탐 |

**예상**: CS_5397 그림에서 파란색이 뇌간·소뇌 **안**에 있었으므로 뇌 바깥 오탐은 1% 미만일 가능성이 높다.
그렇다면 진짜 원인은 **정규화 기준이 배경에 끌려간 것**이다(아래 참조).

## 셀 D-3. 정규화 기준 확인

```python
sample = np.random.default_rng(0).choice(len(te_imgs), 60, replace=False)
vals = {'배경': [], '정상뇌': [], '종양': []}
for i in sample:
    img = te_imgs[i].astype(np.float32)/255.
    f, m = img[:,:,1], te_msks[i] > 0
    bm = brain_mask(img)
    z = (f - MEAN[1]) / STD[1]
    vals['배경'].append(z[~bm].mean())
    if (bm & ~m).sum(): vals['정상뇌'].append(z[bm & ~m].mean())
    if m.sum():         vals['종양'].append(z[m].mean())

print("정규화 후 FLAIR 값 (평균)")
for k, v in vals.items():
    print(f"  {k:6s} {np.mean(v):+7.3f}")
print()
print(f"정상뇌-종양 간격  {np.mean(vals['종양']) - np.mean(vals['정상뇌']):.3f}")
print("-> 간격이 좁으면 밝기 증강 시 정상뇌가 종양 대역으로 밀려 들어간다")
```

---

# 3. 정의 — 새 셀 3개

## 셀 E-1. SegDatasetV2 (기존 SegDataset 대체)

**기존 `[27]` 셀을 지우지 말 것.** 아래에 새로 추가한다.

```python
class SegDatasetV2(Dataset):
    """
    norm_mode : 'global' 기존(train 전체 통계) / 'brain' 뇌 영역만 슬라이스별
    intensity : None 없음 / 'brain' 뇌 안에서만 gain (배경 0 유지)
    """
    def __init__(self, imgs, msks, sub_df, neg_ratio=None, augment=False,
                 norm_mode='global', intensity=None,
                 gain_range=(0.9, 1.1), seed=SEED):
        self.imgs, self.msks, self.sub = imgs, msks, sub_df
        self.neg_ratio, self.augment = neg_ratio, augment
        self.norm_mode, self.intensity = norm_mode, intensity
        self.gain_range = gain_range
        self.pos_idx = np.where(sub_df['tumor'].values == 1)[0]
        self.neg_idx = np.where(sub_df['tumor'].values == 0)[0]
        self.rng = np.random.default_rng(seed)
        self.resample_epoch()

    def resample_epoch(self):
        if self.neg_ratio is None:
            self.rows = np.arange(len(self.sub)); return
        k = min(int(len(self.pos_idx) * self.neg_ratio), len(self.neg_idx))
        pick = self.rng.choice(self.neg_idx, size=k, replace=False)
        self.rows = np.concatenate([self.pos_idx, pick])

    def __len__(self):
        return len(self.rows)

    def __getitem__(self, i):
        j = self.rows[i]
        img = self.imgs[j].astype(np.float32) / 255.0
        msk = self.msks[j].astype(np.float32)

        if self.augment:
            # 기하 변환: 이미지 + 마스크 동일하게
            if self.rng.random() < 0.5: img, msk = img[:, ::-1], msk[:, ::-1]
            if self.rng.random() < 0.5: img, msk = img[::-1],    msk[::-1]
            k = int(self.rng.integers(4))
            if k: img, msk = np.rot90(img, k), np.rot90(msk, k)
            img, msk = np.ascontiguousarray(img), np.ascontiguousarray(msk)

            # 강도 변환: 뇌 안에서만 (배경은 0 유지)
            if self.intensity == 'brain' and self.rng.random() < 0.5:
                bm = brain_mask(img)
                g = self.rng.uniform(*self.gain_range)
                img = img.copy()
                img[bm] = np.clip(img[bm] * g, 0.0, 1.0)

        # 정규화
        if self.norm_mode == 'brain':
            bm = brain_mask(img)
            if bm.sum() > 100:
                m = img[bm].mean(axis=0)
                s = img[bm].std(axis=0) + 1e-6
                img = (img - m) / s
                img[~bm] = -2.0            # 배경 고정
            else:
                img = (img - MEAN) / STD
        else:
            img = (img - MEAN) / STD

        img = np.transpose(img, (2, 0, 1))
        return torch.from_numpy(img).float(), torch.from_numpy(msk)[None].float()

print("SegDatasetV2 정의 완료")
```

### 기존과 무엇이 다른가

| 위치 | 기존 SegDataset | SegDatasetV2 |
|---|---|---|
| 강도 증강 | 없음 | `intensity='brain'` 시 뇌 안에서만 gain |
| 정규화 | 항상 train 전체 통계 | `norm_mode='brain'` 시 슬라이스별 뇌 통계 |
| 배경 처리 | 정규화로 -0.66 | `'brain'` 모드에서 -2.0 고정 |
| 기하 증강 | 동일 | 동일 |
| 재샘플링 | 동일 | 동일 |

`norm_mode='global', intensity=None` 이면 **기존 SegDataset 과 완전히 같다.**

## 셀 E-2. 증강 결과 눈으로 확인

```python
tr_imgs, tr_msks, tr_sub = data['train']
pick = tr_sub['tumor_px'].idxmax()

modes = [('global', None,    '기존'),
         ('global', 'brain', '뇌내 gain'),
         ('brain',  None,    '뇌 정규화'),
         ('brain',  'brain', '둘 다')]

plt.figure(figsize=(15, 9))
for r, (nm, it, label) in enumerate(modes):
    ds = SegDatasetV2(tr_imgs, tr_msks, tr_sub, neg_ratio=None,
                      augment=True, norm_mode=nm, intensity=it, seed=0)
    for c in range(4):
        img, _ = ds[pick]
        f = img[1].numpy()
        corner = f[:20, :20].mean()          # 배경
        plt.subplot(4, 4, r*4 + c + 1)
        plt.imshow(f, cmap='gray'); plt.axis('off')
        plt.title(f"{label}\n배경{corner:+.2f} 범위{f.min():.1f}~{f.max():.1f}",
                  fontsize=7)
plt.tight_layout(); plt.show()
```

**배경 값이 4회 모두 일정한지** 확인. 흔들리면 그게 오탐 원인.

## 셀 E-3. 폴드 분할 함수

```python
def group_kfold_split(df, n_splits=3, seed=SEED):
    """환자를 기관 x 종양비율로 층화한 뒤 폴드에 돌아가며 배정"""
    rng = np.random.default_rng(seed)
    pdf = df.drop_duplicates('patient')[['patient', 'inst']].copy()
    pdf['ratio']  = pdf['patient'].map(df.groupby('patient')['tumor'].mean())
    pdf['bin']    = pd.qcut(pdf['ratio'], q=2, labels=False, duplicates='drop')
    pdf['strata'] = pdf['inst'] + '_' + pdf['bin'].astype(str)

    fold_of = {}
    for st, sub in pdf.groupby('strata'):
        ids = sub['patient'].to_numpy(); rng.shuffle(ids)
        for i, p in enumerate(ids):
            fold_of[p] = i % n_splits
    return df['patient'].map(fold_of)


df['fold'] = group_kfold_split(df, n_splits=3)
print(df.groupby('fold').agg(환자=('patient','nunique'),
                             슬라이스=('patient','size'),
                             종양비율=('tumor','mean')).round(3))
print()
print(pd.crosstab(df.drop_duplicates('patient')['inst'],
                  df.drop_duplicates('patient')['fold']))
```

**확인**: 폴드별 환자 36~37명, 종양비율 0.33~0.36, 기관이 세 폴드에 흩어져야 정상.

---

# 4. 실행 — 교차검증

## 셀 F-1. run_cv_v2

```python
def run_cv_v2(n_splits=3, epochs=20, norm_mode='global', intensity=None,
              brain_constraint=False, tag='CV'):
    global MEAN, STD, data
    df['fold'] = group_kfold_split(df, n_splits=n_splits)
    all_pd, fold_rows = {}, []

    for f in range(n_splits):
        print(f"\n===== Fold {f} =====")
        test_p = set(df.loc[df.fold == f, 'patient'])
        val_p  = set(df.loc[df.fold == (f+1) % n_splits, 'patient'])
        df['split'] = df['patient'].map(
            lambda p: 'test' if p in test_p else ('val' if p in val_p else 'train'))

        fd = {}
        for sp in ['train', 'val', 'test']:
            sub = df[df.split == sp].reset_index(drop=True)
            fd[sp] = (*build_dataset(sub, f'{sp} f{f}'), sub)

        # 정규화 통계 재계산 (이 폴드의 train 에서만)
        s, ss, n = np.zeros(3), np.zeros(3), 0
        for i in range(0, len(fd['train'][0]), 500):
            ch = fd['train'][0][i:i+500].astype(np.float64).reshape(-1,3)/255.
            s += ch.sum(0, dtype=np.float64); ss += (ch**2).sum(0, dtype=np.float64)
            n += len(ch)
        MEAN = (s/n).astype(np.float32)
        STD  = np.sqrt(ss/n - (s/n)**2).astype(np.float32)
        data = fd

        kw_ds = dict(norm_mode=norm_mode, intensity=intensity)
        tr = SegDatasetV2(*fd['train'], neg_ratio=NEG_RATIO, augment=True,  **kw_ds)
        va = SegDatasetV2(*fd['val'],   neg_ratio=None,      augment=False, **kw_ds)
        te = SegDatasetV2(*fd['test'],  neg_ratio=None,      augment=False, **kw_ds)
        kwl = dict(num_workers=NUM_WORKERS, pin_memory=PIN_MEMORY)
        tr_ld = DataLoader(tr, batch_size=BATCH_SIZE, shuffle=True,  **kwl)
        va_ld = DataLoader(va, batch_size=BATCH_SIZE, shuffle=False, **kwl)
        te_ld = DataLoader(te, batch_size=BATCH_SIZE, shuffle=False, **kwl)

        torch.manual_seed(SEED); np.random.seed(SEED)
        model  = UNet(base=BASE, use_bn=False).to(device)
        crit   = nn.BCEWithLogitsLoss()
        opt    = torch.optim.Adam(model.parameters(), lr=LR)
        sched  = torch.optim.lr_scheduler.CosineAnnealingLR(opt, T_max=epochs)
        scaler = torch.amp.GradScaler("cuda", enabled=USE_AMP)
        best, ckpt = -1.0, f'{tag}_f{f}.pt'

        for ep in range(1, epochs+1):
            tr.resample_epoch(); model.train()
            for xb, yb in tr_ld:
                xb, yb = xb.to(device), yb.to(device)
                opt.zero_grad(set_to_none=True)
                with torch.amp.autocast("cuda", enabled=USE_AMP):
                    logits = model(xb)
                loss = crit(logits, yb)
                scaler.scale(loss).backward(); scaler.step(opt); scaler.update()
            sched.step()
            vp_, vt_ = evaluate_seg(model, va_ld)
            vd = float(dice_score(vp_, vt_, 0.5))
            if vd > best:
                best = vd; torch.save(model.state_dict(), ckpt)
            if ep % 5 == 0: print(f"  ep{ep:2d} val_dice {vd:.4f}")
        model.load_state_dict(torch.load(ckpt, map_location=device))

        # 임계값은 이 폴드 val 에서
        vp_, vt_ = evaluate_seg(model, va_ld)
        if brain_constraint:
            vp_ = apply_brain_constraint(vp_, fd['val'][0])
        t = pick_threshold(threshold_sweep(vp_, vt_))

        tp_, tt_ = evaluate_seg(model, te_ld)
        if brain_constraint:
            tp_ = apply_brain_constraint(tp_, fd['test'][0])

        te_sub = fd['test'][2]
        has  = te_sub['tumor'].values.astype(bool)
        pf   = ((tp_ > t).sum(axis=(1,2)) > MIN_PX).astype(int)
        area = (tp_ > t).sum(axis=(1,2)) / IMAGE_SIZE**2

        missed = 0
        for p, gp in te_sub.groupby('patient'):
            k = gp.index.values
            all_pd[p] = float(dice_score(tp_[k], tt_[k], t))
            if ((has[k]) & (pf[k] == 0)).any(): missed += 1

        fold_rows.append({
            'fold': f, 'th': round(t, 2), 'n_test': te_sub['patient'].nunique(),
            'dice': float(dice_score(tp_, tt_, t)),
            'recall': recall_score(has.astype(int), pf, zero_division=0),
            'precision': precision_score(has.astype(int), pf, zero_division=0),
            'prauc': average_precision_score(has.astype(int), area),
            'vol': float(volume_ratio(tp_, tt_, t)),
            'missed': missed,
        })
        print(f"  Fold{f}  Dice {fold_rows[-1]['dice']:.4f}  "
              f"recall {fold_rows[-1]['recall']:.4f}  놓친환자 {missed}")

    return pd.DataFrame(fold_rows), all_pd

print("run_cv_v2 정의 완료")
```

## 셀 F-2. 조건 A (기준)

```python
cvA, pdA = run_cv_v2(n_splits=3, epochs=20,
                     norm_mode='global', intensity=None,
                     brain_constraint=False, tag='CV_A')
```
**25분.** 기존 방식 그대로. 비교 기준이 된다.

## 셀 F-3. 조건 B (대책 적용)

```python
cvB, pdB = run_cv_v2(n_splits=3, epochs=20,
                     norm_mode='brain', intensity='brain',
                     brain_constraint=True, tag='CV_B')
```
**25분.**

> **진단(셀 D-2)에서 뇌 바깥 오탐이 1% 미만이면** `brain_constraint=False` 로 두어도 된다.
> 변수를 줄이면 해석이 명확해진다.

---

# 5. 분석

## 셀 G-1. 폴드 간 안정성

```python
for name, cv in [('A 기존', cvA), ('B 대책', cvB)]:
    print(f"=== {name} ===")
    print(cv[['fold','th','n_test','dice','recall','precision','prauc','vol','missed']]
          .round(4).to_string(index=False))
    print("  폴드 간 편차")
    for col in ['dice','recall','prauc','vol']:
        v = cv[col]
        print(f"    {col:10s} 평균 {v.mean():.4f}  표준편차 {v.std():.4f}  "
              f"범위 {v.min():.4f}~{v.max():.4f}")
    print()
```

**판정 기준**

| 표준편차 | 해석 |
|---|---|
| 0.02 이하 | 안정적. 결론 신뢰 가능 |
| 0.02~0.05 | 보통 |
| 0.05 이상 | 분할 운에 좌우됨. 결론 약함 |

## 셀 G-2. 환자 110명 분포

```python
sA, sB = pd.Series(pdA), pd.Series(pdB)
print(f"{'':14s} {'n':>4} {'평균':>8} {'중앙값':>8} {'표준편차':>8} {'최소':>8} "
      f"{'<0.3':>6} {'<0.5':>6}")
for name, s in [('A 기존', sA), ('B 대책', sB)]:
    print(f"{name:14s} {len(s):>4} {s.mean():>8.4f} {s.median():>8.4f} "
          f"{s.std():>8.4f} {s.min():>8.4f} "
          f"{(s<0.3).sum():>6} {(s<0.5).sum():>6}")
print()
print(f"극단 실패 비율(Dice<0.3)  A {(sA<0.3).mean()*100:.1f}%  "
      f"B {(sB<0.3).mean()*100:.1f}%")
```

**`<0.3` 개수가 가장 중요하다.** 지금까지는 16명 중 1명이라 발생률 추정이 불가했다.
110명이면 신뢰구간이 좁아진다.

## 셀 G-3. 기관별 (처음으로 판단 가능)

```python
for name, s in [('A 기존', sA), ('B 대책', sB)]:
    t_ = s.to_frame('dice')
    t_['inst'] = [p.split('_')[1] for p in t_.index]
    print(f"=== {name} ===")
    print(t_.groupby('inst')['dice'].agg(['count','mean','std','min']).round(4))
    print()
```

지금까지 CS 2명, FG 2명이라 아무 말도 못 했다.
**교차검증 후 CS 16명, FG 14명, EZ 1명 전원이 평가된다.**

## 셀 G-4. 대응표본 검정

```python
common = sorted(set(sA.index) & set(sB.index))
a = np.array([sA[p] for p in common]); b = np.array([sB[p] for p in common])
diff = b - a

# n>22 이면 전수 계산 불가 -> 무작위 순열
rng = np.random.default_rng(SEED)
null = [(rng.choice([-1,1], len(diff)) * np.abs(diff)).mean() for _ in range(100000)]
p_val = float((np.abs(null) >= abs(diff.mean()) - 1e-12).mean())
boot  = [diff[rng.integers(0,len(diff),len(diff))].mean() for _ in range(10000)]
lo, hi = np.percentile(boot, [2.5, 97.5])

print(f"대응표본 순열검정 (환자 n={len(common)})")
print(f"  A 중앙값 {np.median(a):.4f} / B 중앙값 {np.median(b):.4f}")
print(f"  중앙값 차이 {np.median(diff):+.4f}")
print(f"  B가 나은 환자 {(diff>0).sum()} / {len(common)}")
print(f"  p = {p_val:.4f}   95% CI [{lo:+.4f}, {hi:+.4f}]")
```

**n=110이 되어 검정력이 크게 오른다.** 기존 n=16에서는 Bonferroni 보정에 걸렸다.

## 셀 G-5. 시각화

```python
plt.figure(figsize=(13, 4))
plt.subplot(1, 3, 1)
plt.hist([sA.values, sB.values], bins=20, label=['A 기존','B 대책'])
plt.xlabel('환자별 Dice'); plt.ylabel('환자 수'); plt.legend()
plt.title(f'환자 {len(sA)}명 분포')

plt.subplot(1, 3, 2)
for i in range(len(common)):
    plt.plot([0,1], [a[i], b[i]], 'o-', alpha=0.25,
             color='tab:green' if diff[i] > 0 else 'tab:red')
plt.xticks([0,1], ['A 기존','B 대책']); plt.ylabel('환자별 Dice')
plt.title('대응표본'); plt.grid(alpha=0.3)

plt.subplot(1, 3, 3)
plt.boxplot([cvA['dice'], cvB['dice']], tick_labels=['A','B'])
plt.ylabel('폴드별 Dice'); plt.title('폴드 간 편차'); plt.grid(alpha=0.3)
plt.tight_layout(); plt.show()
```

---

# 6. 시간표

| 단계 | 셀 | 소요 | 누적 |
|---|---|---|---|
| 준비 | 기존 15개 | 1분 20초 | 1분 |
| 진단 | D-1 ~ D-3 | 1분 | 2분 |
| 정의 | E-1 ~ E-3 | 30초 | 3분 |
| 조건 A | F-2 | 25분 | 28분 |
| 조건 B | F-3 | 25분 | 53분 |
| 분석 | G-1 ~ G-5 | 1분 | 54분 |

**시간이 부족하면**: 조건 A만 실행. 교차검증 자체가 가이드라인 필수 항목이므로
A만으로도 점수는 확보된다. B는 추가 실험.

---

# 7. 주의사항

**`run_cv_v2` 는 전역 `MEAN`, `STD`, `data` 를 덮어쓴다.**
실행 후 기존 M1 평가를 다시 하려면 셀 `[21]`, `[25]` 를 재실행해야 한다.

**메모리.** 폴드마다 `build_dataset` 으로 배열을 새로 만든다.
`data = fd` 로 교체되면서 이전 배열은 가비지 컬렉션되지만,
불안하면 폴드 끝에 `gc.collect()` 를 넣는다.

**test 재개봉 문제.** 교차검증은 모든 환자가 한 번씩 test 가 되므로
기존 단일 분할의 test 16명도 포함된다.
보고서에는 **"단일 분할 결과가 주 결과, 교차검증은 안정성 검증"** 으로 위계를 명시할 것.
