# 뇌 MRI 종양 분할 프로젝트 — 처음부터 이해하기

이 문서는 **프로젝트를 처음 보는 상태**를 가정하고 쓴 것이다.
발표 대비용 `노트북_셀별_해설.md`와는 목적이 다르다.

읽는 순서
1부 이 프로젝트가 무엇인가
2부 무엇이 어려운가 (모든 설계의 이유)
3부 전체 흐름 7단계
4부 단계별 상세 — 왜 / 안 하면 / 어떻게
5부 실행 체크리스트
6부 용어 사전

---

# 1부. 이 프로젝트가 무엇인가

## 한 문장

**뇌 MRI 사진을 넣으면 종양이 있는 부분을 픽셀 단위로 칠해주는 모델을 만든다.**

## 지금까지 배운 것과 무엇이 다른가

수업에서 세 가지를 배웠다.

| 노트북 | 문제 | 입력 | 출력 |
|---|---|---|---|
| 2번 | **분류** | 사진 1장 | "고양이" (라벨 1개) |
| 3번 | **객체탐지** | 사진 1장 | "여기 헬멧" (네모 상자) |
| 4번 | **분류** (전이학습) | X-ray 1장 | "폐렴" / "정상" |
| 5번 | **분할** | 도로 사진 1장 | 픽셀마다 라벨 (30종류) |

이번은 **5번과 같은 분할**인데, 라벨이 30개가 아니라 **2개(종양/배경)** 다.
그래서 "이진 분할(binary segmentation)"이라 부른다.

```
분류    사진 -> 숫자 1개        "이 사진에 종양이 있다"
분할    사진 -> 사진 1장         "여기 이 부분이 종양이다"
```

출력이 숫자가 아니라 **같은 크기의 흑백 그림**이라는 게 핵심이다.
256×256 사진을 넣으면 256×256짜리 답이 나온다.

## 데이터가 어떻게 생겼나

```
kaggle_3m/
├── TCGA_CS_4941_19960909/          <- 환자 1명
│   ├── TCGA_CS_4941_19960909_1.tif        원본 (뇌를 가로로 자른 1번째 사진)
│   ├── TCGA_CS_4941_19960909_1_mask.tif   정답 (그 사진의 종양 위치)
│   ├── TCGA_CS_4941_19960909_2.tif
│   ├── TCGA_CS_4941_19960909_2_mask.tif
│   └── ...  (평균 36장)
├── TCGA_CS_4942_19970222/          <- 환자 2명째
└── ...  (총 110명)
```

**환자 한 명 = 폴더 하나**다. 그 안에 뇌를 위에서 아래로 얇게 썬 사진들이 번호순으로 있다.
CT나 MRI는 이렇게 층층이 찍는다. 한 장 한 장을 **슬라이스(slice)** 라고 한다.

```
총 환자      110명
총 슬라이스   3,929장
사진 크기     256 x 256, 3채널
```

**3채널이 RGB가 아니다.** 컬러 사진이 아니라, 같은 부위를 세 가지 방식으로 찍은 것을 쌓아둔 거다.

```
채널 0   pre-contrast    조영제 넣기 전
채널 1   FLAIR           뇌척수액 신호를 억제한 촬영 -> 종양이 밝게 보임
채널 2   post-contrast   조영제 넣은 후
```

정답 마스크는 **FLAIR에서 이상하게 보이는 부분**을 의사가 칠한 것이다.
그래서 FLAIR 채널이 가장 중요하다.

마스크는 1채널이고 값이 0 아니면 255다. 종양이면 255(흰색), 아니면 0(검정).

## 최종 목표

**임상 전제** — 의사가 환자 1명의 MRI를 판독하는 상황.
그래서 "평균 점수 최대화"가 아니라 **"이 환자의 종양을 놓치지 않는 것"** 이 목표다.

---

# 2부. 무엇이 어려운가

**이 프로젝트의 모든 설계는 아래 세 가지 문제에서 나온다.**
이 부분을 이해하면 나머지는 전부 따라온다.

## 어려움 1 — 종양이 너무 작다 (클래스 불균형)

```
전체 픽셀 중 종양 픽셀    1.03%
종양이 있는 슬라이스       34.9%  (나머지 65.1%는 종양 없음)
가장 작은 종양            7픽셀   (65,536픽셀 중)
```

**"전부 배경이야"라고만 답해도 98.84% 정확하다.**

실제로 확인했다. 아무것도 예측하지 않는 모델의 픽셀 정확도가 0.9884다.
그러니까 **정확도(accuracy)라는 지표를 쓰면 안 된다.**

그리고 모델도 게을러진다. "전부 배경"이라고 답하면 손실이 이미 낮으니,
굳이 종양을 찾을 이유가 없다. 학습이 그 상태에서 멈출 수 있다.

**여기서 나오는 설계**
- 지표를 Dice로 바꾼다 (3부 6단계)
- 손실함수에 Dice 항을 섞는다 (3부 5단계)
- 빈 슬라이스를 덜 넣는다 (3부 4단계)

## 어려움 2 — 한 환자에게 사진이 여러 장이다 (데이터 누수)

환자 한 명당 슬라이스가 평균 36장이다. **3번 슬라이스와 4번 슬라이스는 거의 같은 그림이다.**
뇌를 1mm 간격으로 썰었으니 당연하다.

이걸 모르고 3,929장을 랜덤으로 섞어서 나누면 이렇게 된다.

```
train에 TCGA_CS_4941의 3번 슬라이스
test에  TCGA_CS_4941의 4번 슬라이스
```

모델이 학습에서 본 그림을 시험에서 또 보는 것이다. 점수가 부풀려진다.

**실제로 해봤더니 test 환자 109명 전원이 train에도 들어갔다.**

**여기서 나오는 설계**
- 사진이 아니라 **환자 단위로** 나눈다 (3부 2단계)

## 어려움 3 — 병원마다 사진이 다르다 (도메인 시프트)

환자 ID에 병원 코드가 들어 있다. `TCGA_CS_4941_19960909`에서 `CS`가 병원이다.

```
병원   환자수   FLAIR 평균 밝기
DU      45명      0.1905
HT      34명      0.2363
CS      16명      0.2445    <- DU보다 28% 밝다
FG      14명      0.2396
EZ       1명      0.2506
```

MRI 기계와 촬영 설정이 병원마다 달라서 밝기가 다르다.
DU 병원 사진만 배운 모델은 CS 병원 사진에서 성능이 떨어질 수 있다.

**여기서 나오는 설계**
- 나눌 때 병원을 고르게 섞는다 (3부 2단계)
- 밝기 증강을 넣는 것도 검토 (3부 4단계)

---

# 3부. 전체 흐름 7단계

```
[1] 데이터 정리      사진 목록을 표로 만들고, 종양이 있는지 세어본다
        |
[2] 데이터 분할      환자 단위로 train / val / test 로 나눈다
        |
[3] 메모리 적재      3,929장을 RAM에 올리고 정규화 기준을 만든다
        |
[4] Dataset 만들기   모델에 먹일 수 있는 형태로 포장 + 증강
        |
[5] 모델과 손실      U-Net 구조 정의 + 무엇을 틀렸다고 볼지 정의
        |
[6] 학습             30에폭 돌리면서 가장 좋은 상태를 저장
        |
[7] 평가             임계값 정하고, 3층위로 채점하고, 통계 검정
```

## 세 덩어리로 나누면

**A. 데이터 준비 (1~4단계)** — 전체 시간의 절반
사진을 모델이 먹을 수 있는 형태로 만드는 과정. 지루하지만 **여기서 실수하면 뒤가 전부 무의미해진다.**

**B. 모델 학습 (5~6단계)** — 실제로는 10분
U-Net을 정의하고 돌린다. 코드는 이미 5번 노트북에서 배운 것이다.

**C. 평가 (7단계)** — 여기가 이 프로젝트의 차별점
숫자 하나 뽑고 끝내는 게 아니라, **어떤 숫자를 왜 믿을 수 있는지**를 보인다.

## train / val / test 가 무엇인가

세 덩어리로 나누는 이유를 헷갈리기 쉬우니 정리한다.

| | 이름 | 하는 일 | 비유 |
|---|---|---|---|
| train | 학습 | 모델이 보고 배운다 | 교과서 |
| val | 검증 | 학습 중에 성적을 보고 설정을 정한다 | 모의고사 |
| test | 시험 | **딱 한 번만** 열어서 최종 점수를 낸다 | 수능 |

**test를 여러 번 보면서 설정을 바꾸면 그건 부정행위다.**
test에 맞춰 조정한 결과라 실제 성능이 아니게 된다.

우리 프로젝트에서는 이렇게 나눴다.

```
train   환자 78명   2,742장
val     환자 16명     565장
test    환자 16명     622장
```

---

# 4부. 단계별 상세

각 단계마다 **왜 필요한가 / 안 하면 어떻게 되나 / 어떻게 하나** 순서로 정리한다.

---

## [1단계] 데이터 정리

### 왜 필요한가
파일이 110개 폴더에 흩어져 있다. 어떤 사진이 어느 환자 것이고 종양이 있는지 없는지를
한눈에 볼 수 있는 **표**가 있어야 이후 작업이 가능하다.

### 안 하면
매번 `glob`으로 파일을 뒤져야 하고, 환자별 통계를 낼 수 없다.

### 어떻게 하나

**(1) 파일 목록을 데이터프레임으로**

```python
for mask_path in glob(os.path.join(ROOT, "*", "*_mask.tif")):
    img_path = mask_path.replace("_mask.tif", ".tif")
    patient  = os.path.basename(os.path.dirname(mask_path))
    slice_no = int(os.path.basename(img_path).split("_")[-1].replace(".tif", ""))
```

**마스크 파일부터 찾는 이유가 있다.** 원본과 마스크가 같은 폴더에 있는데,
`*.tif`로 찾으면 둘 다 걸린다. 마스크는 `_mask.tif`로 이름이 특정되니
마스크를 먼저 잡고 `_mask`만 떼면 원본 경로가 나온다. 실수할 여지가 없다.

**(2) 무결성 검증**

```python
if problems:
    raise ValueError("무결성 검증 실패: ...")
```

파일이 하나라도 빠지면 여기서 멈춘다. `print`만 하면 사람이 못 보고 지나쳐서
나중에 엉뚱한 곳에서 오류가 난다. **문제를 빨리 터뜨리는 게 낫다.**

**(3) 종양 유무 표시**

```python
def tumor_stats(mask_path):
    m = cv2.imread(mask_path, cv2.IMREAD_GRAYSCALE)
    return pd.Series({"tumor": int(m.max() > 0), "tumor_px": int((m > 0).sum())})
```

마스크를 열어서 흰 픽셀이 하나라도 있으면 `tumor=1`, 개수는 `tumor_px`에 넣는다.

여기서 **1.03%** 라는 숫자가 나온다. 2부의 어려움 1이 확인되는 순간이다.

**(4) 병원 코드 추출**

```python
df["inst"] = df["patient"].str.split("_").str[1]
```

`TCGA_CS_4941_19960909` → `CS`. 문자열 자르기만으로 병원 정보를 얻는다.
2부의 어려움 3에 쓴다.

---

## [2단계] 데이터 분할 — 가장 중요한 단계

### 왜 필요한가
모델을 공정하게 평가하려면, **모델이 한 번도 못 본 데이터**로 시험을 봐야 한다.

### 안 하면
2부의 어려움 2가 그대로 터진다. test 환자 109명 전원이 train에 들어가서
점수가 0.1~0.15 정도 부풀려진다. **논문이나 보고서에 못 쓰는 결과가 된다.**

### 어떻게 하나

**(1) 4번 노트북 방식을 그대로 쓰면 안 된다**

```python
# 4번 노트북 (흉부 X-ray)
perm = torch.randperm(n_total).tolist()
val_idx, train_idx = perm[:n_val], perm[n_val:]
```

거기서는 **맞는 방법이었다.** X-ray는 한 장이 환자 한 명이니까.
여기는 한 명이 36장이라 안 된다.

**(2) 환자를 섞고, 그 환자의 모든 슬라이스를 통째로 보낸다**

```python
pdf = df.drop_duplicates("patient")     # 환자 목록 (110행)
rng.shuffle(ids)                         # 환자를 섞는다
for p in ids[:n_te]:  assign[p] = "test" # 환자 단위로 배정
df["split"] = df["patient"].map(assign)  # 슬라이스에 전파
```

**(3) 층화(stratification)가 무엇인가**

그냥 무작위로 섞으면 운이 나쁠 때 이런 일이 생긴다.

- 종양이 많은 환자가 전부 train에 몰림
- CS 병원 환자가 test에 한 명도 없음 (실제로 초기 분할에서 일어났다)

**층화란 "비슷한 것끼리 묶어서 각 묶음마다 따로 나누는 것"** 이다.

```python
pdf["bin"]    = pd.qcut(pdf["ratio"], q=2, labels=False)   # 종양 비율 상/하
pdf["strata"] = pdf["inst"] + "_" + pdf["bin"].astype(str) # 병원 x 종양비율
```

```
DU_0 (DU병원 + 종양적음)  25명  ->  이 안에서 15%씩 떼서 배분
DU_1 (DU병원 + 종양많음)  20명  ->  이 안에서 15%씩 떼서 배분
CS_0 ...
```

이렇게 하면 어느 묶음이든 세 세트에 고르게 들어간다.

**(4) 층을 너무 잘게 쪼개면 안 된다**

지금 병원 5 × 종양비율 2 = 최대 10층이다. 110명을 10층으로 나누면 층당 11명.
여기서 15%면 1.65명이라 아슬아슬하다.

종양 크기까지 넣으면 20층, 층당 5.5명, 15%면 0.8명 → 반올림하면 0명인 층이 생긴다.
**층화는 세밀할수록 좋은 게 아니다.**

EZ 병원은 환자가 1명뿐이라 아예 나눌 수 없어서 train에 고정했다.

**(5) 층화 안 한 것은 사후 검증한다**

```python
chk = pd.DataFrame({
    "종양크기 중앙값(px)": pos.groupby("split")["tumor_px"].median(), ...
})
```

결과: 중앙값은 균형(1,487 / 1,766 / 1,600)이지만
**val의 최소 종양이 37px로 가장 크다.** val에 작은 종양이 적다는 뜻이고,
이건 7단계 임계값 결정에 영향을 준다.

**(6) 누수 검사**

```python
assert leak == 0, "데이터 누수 발생"
```

세 세트 간 환자 교집합이 0인지 확인. 하나라도 겹치면 멈춘다.

---

## [3단계] 메모리 적재

### 왜 필요한가
매 에폭마다 3,929개 파일을 디스크에서 다시 읽으면 느리다.
tif 디코딩이 생각보다 무겁다.

### 안 하면
에폭 시간이 몇 배로 늘어난다. 3분 기준을 못 맞출 수 있다.

### 어떻게 하나

**(1) uint8로 통째로 올린다**

```python
imgs = np.zeros((len(sub_df), 256, 256, 3), dtype=np.uint8)
```

**uint8인 게 중요하다.** float32로 올리면 4배(3.8GB)가 된다.
uint8이면 0.96GB라 부담이 없고, 학습할 때만 float으로 바꾸면 된다.

**(2) 채널 순서 되돌리기**

```python
imgs[i] = cv2.imread(r["image"], cv2.IMREAD_UNCHANGED)[:, :, ::-1]
```

`cv2`는 역사적인 이유로 색을 **BGR 순서**로 읽는다.
`[:, :, ::-1]`이 순서를 뒤집어 원래대로(pre / FLAIR / post) 돌린다.
안 하면 FLAIR가 가운데 있는 건 같지만 pre와 post가 바뀐다.

**(3) 정규화 기준 만들기**

정규화란 **픽셀 값을 평균 0, 표준편차 1 근처로 맞추는 것**이다.
안 하면 학습이 불안정해진다. (2번 노트북에서 `transforms.Normalize`로 했던 것)

```python
MEAN = (s / n).astype(np.float32)     # [0.0858, 0.0822, 0.0891]
STD  = np.sqrt(ss / n - (s/n)**2)     # [0.1302, 0.1223, 0.1301]
```

**4번 노트북의 ImageNet 값을 쓰면 안 된다.**

```
ImageNet   [0.485, 0.456, 0.406]
우리 데이터 [0.0858, 0.0822, 0.0891]   <- 5분의 1
```

4번은 사전학습 모델을 썼으니 그 모델이 배운 통계에 맞춰야 했다. 옳은 선택이었다.
우리는 밑바닥부터 학습하고, 게다가 MRI는 뇌 바깥이 전부 검은 배경이라 평균이 낮다.

**train에서만 계산하는 이유** — val/test까지 포함하면 데이터 누수다.
표 데이터에서 `StandardScaler`를 test 포함해서 `fit`하면 안 되는 것과 같은 원리.

**float64로 더하는 이유** — 5억 개 픽셀을 float32로 더하면
나중 값이 반올림에 묻혀 평균이 2% 틀어진다. (실측: 0.0966 vs 0.0992)

---

## [4단계] Dataset 만들기

### 왜 필요한가
파이토치는 데이터를 `Dataset` 클래스로 감싸야 `DataLoader`로 배치를 만들 수 있다.
5번 노트북에서 이미 해본 구조다.

### 안 하면
배치 처리, 셔플, 증강을 전부 직접 짜야 한다.

### 어떻게 하나

**(1) `__getitem__`이 하는 일**

```python
def __getitem__(self, i):
    img = self.imgs[j].astype(np.float32) / 255.0   # 0~255 -> 0~1
    msk = self.msks[j].astype(np.float32)            # 0/1
    ... 증강 ...
    img = np.transpose((img - MEAN) / STD, (2, 0, 1))  # 정규화 + 축 순서 변경
    return torch.from_numpy(img).float(), torch.from_numpy(msk)[None].float()
```

`np.transpose(..., (2,0,1))`은 `(256,256,3)`을 `(3,256,256)`으로 바꾼다.
numpy는 채널이 뒤에, 파이토치는 채널이 앞에 오는 관례를 쓴다.

`[None]`은 마스크에 차원을 하나 추가해서 `(256,256)`을 `(1,256,256)`으로 만든다.
출력이 1채널이므로 정답도 1채널이어야 한다.

**(2) 5번과 달라진 곳 — 마스크 자료형**

```python
# 5번 (30클래스)
mask = torch.from_numpy(self.masks[i].astype(np.int64))   # (H,W), 값 = 0~29
# 이번 (이진)
torch.from_numpy(msk)[None].float()                        # (1,H,W), 값 = 0.0/1.0
```

`CrossEntropyLoss`는 "몇 번 클래스인가"를 정수로 받는다.
`BCEWithLogitsLoss`는 "종양일 확률"을 실수로 받는다. 그래서 자료형이 다르다.

**(3) 빈 슬라이스 재샘플링 — 이 프로젝트의 핵심 아이디어**

문제 상황
```
train 원본:  종양 947장 + 빈 슬라이스 1,795장   (빈 게 65%)
```

빈 슬라이스가 너무 많으면 모델이 "전부 배경"으로 기운다.
그렇다고 다 빼면, 실제 추론에서 정상 슬라이스에 종양을 그려댄다.

해결
```python
def resample_epoch(self):
    k = min(int(len(self.pos_idx) * self.neg_ratio), len(self.neg_idx))
    pick = self.rng.choice(self.neg_idx, size=k, replace=False)
    self.rows = np.concatenate([self.pos_idx, pick])
```

**매 에폭마다 빈 슬라이스를 새로 뽑는다.**

```
1에폭:  종양 947장 + 빈 슬라이스 A그룹 947장
2에폭:  종양 947장 + 빈 슬라이스 B그룹 947장
3에폭:  종양 947장 + 빈 슬라이스 C그룹 947장
...
```

30에폭을 돌면 1,795장을 결국 거의 다 보게 된다.
**데이터를 버리는 게 아니라 한 에폭 안의 비율만 조절**하는 것이다.

`NEG_RATIO=1.0`이면 종양:빈 = 1:1이 되어 불균형이 65%에서 50%로 줄어든다.

이 효과가 실제로 확인됐다. M1(BCE 손실만 쓴 베이스라인)이
**1에폭 만에 val Dice 0.5418**을 냈다. 재샘플링 없이는 나올 수 없는 숫자다.

**val/test는 재샘플링하지 않는다.** (`neg_ratio=None`)
평가는 실제 분포에서 해야 의미가 있다.

**(4) 증강 — 왜 transforms를 못 쓰나**

증강이란 **같은 사진을 조금씩 바꿔서 데이터를 늘리는 것**이다.
뒤집고 돌리면 새로운 학습 데이터가 된다.

2번, 4번에서는 이렇게 했다.
```python
transforms.RandomHorizontalFlip()
```

**분할에서는 못 쓴다.** 이건 이미지 하나만 받아서 뒤집는다.
마스크는 그대로 남아서 **정답이 어긋난다.**

```
이미지 뒤집음:   종양이 오른쪽으로 이동
마스크 그대로:   정답은 여전히 왼쪽
-> 모델이 잘못 배운다
```

그래서 직접 짰다.
```python
if self.rng.random() < 0.5:
    img, msk = img[:, ::-1], msk[:, ::-1]   # 둘 다 뒤집는다
```

검증했더니 2,000회 시행에서 불일치 0건, 8가지 방향이 12.1~12.7%로 고르게 나왔다.

**중요한 구분**
```
기하 변환 (뒤집기, 회전)   -> 이미지 + 마스크 둘 다
강도 변환 (밝기, 대비)     -> 이미지만  (종양 위치는 밝기와 무관)
정규화                     -> 이미지만
```

---

## [5단계] 모델과 손실함수

### 왜 U-Net인가

분할에는 문제가 하나 있다. **CNN은 층을 지날수록 해상도를 줄인다.**

```
256x256 -> 128 -> 64 -> 32 -> 16
```

이래야 넓은 범위를 볼 수 있다. 그런데 출력은 다시 256×256이어야 한다.
16×16에서 256×256으로 되돌리면 **위치 정보가 뭉개진다.**

U-Net의 해법이 **skip connection**이다.

```
인코더 (줄이기)          디코더 (키우기)
256 ──────────────────────> 256      <- 이 화살표가 skip connection
 |                           ^
128 ──────────────────────> 128
 |                           ^
 64 ──────────────────────>  64
 |                           ^
 32 ──────────────────────>  32
 |                           ^
 16 ────> bottleneck ────>  16
```

인코더에서 줄이기 전의 고해상도 정보를 디코더로 직접 보낸다.
`torch.cat`으로 이어붙이는 게 그거다.

**7픽셀짜리 종양을 잡으려면 이게 필수다.** skip connection 없이는
16×16까지 줄인 정보만으로 7픽셀 위치를 복원할 수 없다.

### 5번과 달라진 두 곳

**(1) 출력 채널 30 → 1**

```python
self.out = nn.Conv2d(b, n_classes, 1)   # n_classes = 1
```

30개 클래스 중 고르는 게 아니라 "종양일 확률" 하나만 낸다.
4번에서 `nn.Linear(128, 1)`로 이진 출력을 만든 것과 같은 원리다.

예측 방법도 바뀐다.
```python
# 5번:  pred = logits.argmax(1)          가장 확률 높은 클래스
# 이번: pred = sigmoid(logits) > 0.5     확률이 기준을 넘으면 종양
```

**(2) BatchNorm 추가**

2번 노트북에서 배운 것이다. 각 층의 출력을 정규화해 학습을 안정시킨다.
Cityscapes에서는 없어도 됐지만, 종양 픽셀 1%인 여기서는 초반 수렴이 불안정하다.

`use_bn=False`로 두면 5번 원본과 사실상 같아진다.
파라미터가 2,160,177 대 2,158,705로 **0.07%만 다르다.**
이걸 베이스라인 M1으로 쓴다.

### 손실함수 — 무엇을 "틀렸다"고 볼 것인가

**손실함수란 모델의 답과 정답이 얼마나 다른지를 숫자 하나로 만드는 것**이다.
이 숫자가 작아지는 방향으로 모델이 학습한다.

**BCE만 쓰면 위험한 이유**

BCE(Binary Cross Entropy)는 픽셀 하나하나를 독립적으로 채점한다.
종양이 1%뿐이니 **"전부 배경"이라고 답해도 99%를 맞힌 셈**이라 손실이 이미 낮다.
경사하강법 입장에서 종양을 찾을 이유가 없다.

**Dice가 무엇인가**

두 영역이 얼마나 겹치는지를 재는 값이다.

```
Dice = 2 x (겹친 부분) / (예측 크기 + 정답 크기)

완전 일치  -> 1.0
전혀 안 겹침 -> 0.0
```

**Dice는 배경을 아예 세지 않는다.** 그래서 "전부 배경" 전략이 통하지 않는다.
1%짜리 종양을 못 찾으면 Dice가 0이다.

**그래서 둘을 섞는다**

```python
return self.w * bce + (1 - self.w) * dice    # 0.5씩
```

BCE는 안정적이지만 불균형에 약하고, Dice는 불균형에 강하지만 초반이 불안정하다.
서로의 단점을 상쇄한다.

**smooth=1.0의 역할**

빈 마스크(정답이 전부 0)에서 예측도 0이면 `0/0`이 되어 계산이 깨진다.
분자와 분모에 1을 더하면 `(0+1)/(0+1) = 1`이 되어 "잘 맞췄다"로 처리된다.
**빈 슬라이스를 학습에 넣을 수 있는 근거가 이것이다.**

**Tversky — 놓침에 벌점을 더 주는 손실**

```python
Tversky = TP / (TP + alpha*FP + beta*FN)

TP  맞게 찾은 종양 픽셀
FP  종양이 아닌데 종양이라 한 것 (오탐)
FN  종양인데 놓친 것          (놓침)
```

`beta`를 키우면 FN(놓침)의 벌점이 커진다. → 모델이 마스크를 넓게 그린다 → 재현율 상승.
`alpha=beta=0.5`면 Dice와 같아진다.

**임상 전제(놓친 종양 최소화)를 손실함수에 반영한 것**이 M3다.

**AMP 함정 — logits.float()**

AMP(혼합정밀도)는 계산을 float16으로 해서 속도를 올린다. 그런데

```
float16 최댓값      65,504
256 x 256 픽셀 수   65,536    <- 이미 초과
```

Dice 계산에서 픽셀을 다 더하면 이 한계를 넘어 무한대가 된다.
그러면 **손실이 정확히 1.0에 고정되고 학습이 멈춘다.**
오류도 안 나고 nan도 안 뜬다. 그냥 조용히 안 배운다.

그래서 손실 안에서 `logits.float()`으로 float32로 되돌린다.

---

## [6단계] 학습

### 학습 루프가 하는 일

```python
for epoch in range(1, epochs + 1):
    tr_ds.resample_epoch()              # 빈 슬라이스 새로 뽑기
    for xb, yb in tr_ld:                # 배치 단위로
        logits = model(xb)              # 1. 예측
        loss = crit(logits, yb)         # 2. 얼마나 틀렸나
        loss.backward()                 # 3. 각 가중치를 어느 방향으로 고칠지 계산
        opt.step()                      # 4. 실제로 고침
```

**에폭(epoch)** — 학습 데이터를 처음부터 끝까지 한 바퀴 도는 것. 30에폭이면 30바퀴.
**배치(batch)** — 한 번에 처리하는 사진 묶음. 16장씩 묶으면 1,894장이 119묶음.

### 5번과 달라진 세 곳

**(1) 체크포인트 기준이 뒤집혔다**

```python
# 5번:  if val_loss < best_val    (손실이 낮을수록 좋다)
# 이번: if va_dice > best_dice    (Dice가 높을수록 좋다)
```

**이게 왜 중요하냐면**, val_loss나 픽셀 정확도로 저장하면
"전부 배경" 모델이 최고점으로 남기 때문이다.

**체크포인트란** 학습 중 가장 좋았던 시점의 가중치를 파일로 저장해두고,
끝나면 그 상태로 되돌리는 것이다. 30에폭을 돌아도 24에폭이 최고면 24에폭 상태를 쓴다.

**(2) train Dice도 기록한다**

과적합을 보려면 loss만으로는 부족하다.

**과적합(overfitting)이란** 모델이 학습 데이터를 외워버려서
처음 보는 데이터에서는 성능이 떨어지는 현상이다.

M1 결과
```
에폭 10:  train 0.6863  val 0.7228   격차 -0.0365
에폭 30:  train 0.8721  val 0.7262   격차 +0.1459   <- 과적합 진행
```

train은 계속 오르는데 val은 17에폭부터 멈췄다. 격차가 벌어지는 게 과적합 신호다.

**(3) AMP 추가**

```python
with torch.amp.autocast("cuda", enabled=USE_AMP):
    logits = model(xb)          # 순전파는 float16 (빠름)
loss = crit(logits, yb)         # 손실은 float32 (안전)
scaler.scale(loss).backward()   # GradScaler가 값을 키워서 역전파
```

**GradScaler가 필요한 이유** — float16은 작은 값도 표현 못 한다.
그래디언트가 너무 작으면 0이 되어 사라진다.
손실에 65,536을 곱해 크게 만든 뒤 역전파하고, 나중에 되돌린다.

---

## [7단계] 평가

### 임계값(threshold)이란

모델은 픽셀마다 "종양일 확률"을 0~1 사이 숫자로 낸다.
어디부터 종양이라고 할지 선을 그어야 한다. 그게 임계값이다.

```
확률 0.7 -> 임계값 0.5면 종양,  임계값 0.8이면 배경
```

**0.5는 관습일 뿐 근거가 없다.** 종양이 희소하면 더 낮은 값이 최적일 수 있다.

**중요한 사실** — 임계값을 바꾸면 점수가 크게 움직이지만,
**그건 모델이 좋아진 게 아니다.** 정밀도-재현율 곡선 위에서 위치만 옮긴 것이다.
임계값과 무관한 지표(PR-AUC)는 전혀 변하지 않는다.

그래서 **모델끼리 비교할 때는 각자의 최적 임계값에서** 해야 공정하다.

**val에서 정하고 test에 고정 적용한다.** test를 보고 고르면 부정행위다.

**최고점이 아니라 고원 중앙을 잡는 이유** — val 환자가 16명뿐이라
뾰족한 최고점은 우연일 수 있다. 게다가 2단계에서 확인했듯
**val에는 작은 종양이 적어서** 임계값이 높게 잡히는 경향이 있다.

### 지표 3층위

**층위 1 — 픽셀 수준**
```
Dice      겹침 정도 (주 지표)
IoU       비슷하지만 더 엄격
체적비     예측 크기 / 정답 크기. 1.0이 정확
```

**체적비가 임상적으로 중요하다.** Dice가 같아도
과대평가와 과소평가는 의미가 정반대다. 저등급 신경교종은 추적 관찰하는 경우가 많아
크기를 잘못 재면 "커졌다"는 오판을 부른다.

**층위 2 — 슬라이스 수준 (4번 노트북 지표)**

"이 슬라이스에 종양이 있나?"를 이진 분류로 본다.
```
recall     놓친 종양이 얼마나 적은가   <- 임상적으로 최우선
precision  오탐이 얼마나 적은가
F1         둘의 균형
PR-AUC     임계값과 무관한 종합 점수
```

**accuracy는 뺐다.** 아무것도 안 해도 0.9884가 나오니까.

**층위 3 — 환자 수준**

환자별 Dice + **놓친 슬라이스 번호**.

평균 Dice 0.83은 의사에게 아무 지시도 주지 못하지만,
"18번 슬라이스를 다시 보라"는 실행 가능한 정보다.
**임상 전제의 핵심 산출물이다.**

### 왜 M0(규칙 기반)를 먼저 만들었나

학습 없이 FLAIR 밝기에 임계값만 걸어본 것이다.

```
M0 Otsu        Dice 0.1487  체적 3.64배  슬라이스 recall 1.0000  precision 0.3671
전부 배경       Dice 0.0000  픽셀 정확도 0.9884
항상 종양 있음   슬라이스 recall 1.0000  precision 0.3441
```

**이 표가 두 지표의 무용함을 증명한다.**

1. **정확도** — 아무것도 안 해도 0.9884
2. **재현율 단독** — M0는 슬라이스 재현율 1.0인데 체적을 3.64배 과대평가한다.
   종양을 다 찾긴 했지만 크기가 3.6배라 쓸 수 없다.
   → **재현율은 목표가 아니라 제약 조건이어야 한다.**

그리고 딥러닝의 개선 폭도 정량화된다. `0.1487 → 0.7327`, 약 5배.

### 가설검정 — 왜 필요한가

M2가 M3보다 Dice가 0.02 높다고 하자. **이게 진짜 차이인가 우연인가?**

동전을 10번 던져 앞면이 6번 나왔다고 그 동전이 이상한 건 아니다.
마찬가지로 Dice 0.02 차이가 우연일 수 있다. 통계로 판단해야 한다.

**검정 단위가 환자(n=16)여야 하는 이유**

슬라이스 622개로 검정하고 싶어지지만 안 된다.
같은 환자의 인접 슬라이스는 거의 같은 그림이라 **독립 표본이 아니다.**
독립성이 깨지면 p값이 실제보다 훨씬 작게 나와서,
**아무 차이 없는 두 모델도 p < 0.001이 나온다.**

**대응표본(paired)이란**

같은 환자를 두 모델이 각각 평가한다. 환자 A에서 M2가 0.80, M3가 0.83.
이렇게 짝을 지으면 환자마다 다른 난이도(종양 크기 등)가 상쇄되어
표본이 적어도 검정력이 생긴다.

**순열검정을 쓰는 이유**

Dice는 0~1에 갇혀 있고 분포가 치우쳐서 t검정의 정규성 가정이 안 맞는다.
순열검정은 분포 가정이 필요 없다.

환자 16명이면 부호 조합이 2^16 = 65,536가지라 **전수 계산이 가능하다.**
근사가 아니라 정확한 p값이 나온다. `scipy` 없이 numpy만으로 구현했다.

**해석 주의**
- `p > 0.05`는 "차이 없음"이 아니라 **"차이를 확인하지 못함"**
- p값만 쓰지 말고 중앙값 차이, 효과크기, 신뢰구간을 함께 보고

---

# 5부. 실행 체크리스트

```
[ ] 0-1  device: cuda 확인          <- cpu면 30배 느림
[ ] 0-2  BASE=32, BATCH=16, EPOCHS=30
[ ] 0-3  env.json 생성

[ ] 1-0  ROOT 경로 1개만 잡히는지    <- 2개면 zip을 두 번 푼 것
[ ] 1-0  [검증 통과] 출력            <- 환자 110 / 원본 3929 / 마스크 3929
[ ] 1-1  종양 픽셀 1.03% 확인
[ ] 1-2  기관 5개 밝기 차이 확인
[ ] 1-3  시각화 — 종양이 빨갛게 보이는지

[ ] 2-0  환자 교집합 0 확인          <- assert가 잡아줌
[ ] 2-1  잘못된 방식 109명 중복 확인
[ ] 2-2  층화 사후 검증 표

[ ] 3-0  적재 0.96GB / 3초
[ ] 3-1  MEAN이 0.08 근처인지        <- ImageNet 0.485와 다름

[ ] 4-0  마스크 값 [0.0, 1.0] 확인
[ ] 4-0  에폭당 학습 장수 1,894 확인

[ ] 5-0  출력 shape (1,1,256,256)
[ ] 6-1  Tversky beta 검증표
[ ] 6-3  속도 측정 — 180초 이내

[ ] 7-0  M0 Dice 0.1487
[ ] 9-0  M1 학습 (약 10분)           <- 완료: val Dice 0.7327
[ ] 9-1  M2 학습
[ ] 9-2  M3 학습
[ ] 9-3  학습 곡선 3장

[ ] 10   임계값 결정
[ ] 11   평가 3층위
[ ] 12   예측 시각화
[ ] 13   가설검정
[ ] 14   results.json 저장
```

**주의 — 1번 셀을 다시 실행하면 `df`가 초기화된다.**
`tumor`, `tumor_px`, `split`이 전부 사라지므로 1 → 1-1 → 2 순서로 다시 돌려야 한다.

---

# 6부. 용어 사전

| 용어 | 뜻 | 이 프로젝트에서 |
|---|---|---|
| 슬라이스 | 뇌를 가로로 자른 사진 1장 | 총 3,929장 |
| 마스크 | 정답. 종양 위치를 칠한 흑백 그림 | 0 또는 255 |
| 분할 | 픽셀마다 라벨을 매기는 것 | 종양/배경 2종 |
| 클래스 불균형 | 한쪽 라벨이 압도적으로 많음 | 종양 1.03% |
| 데이터 누수 | 시험 문제가 교과서에 있는 상황 | 환자 단위 분할로 방지 |
| 층화 | 비슷한 것끼리 묶어 각 묶음마다 나눔 | 병원 × 종양비율 |
| 에폭 | 학습 데이터 한 바퀴 | 30바퀴 |
| 배치 | 한 번에 처리하는 묶음 | 16장 |
| 증강 | 사진을 변형해 데이터를 늘림 | 뒤집기, 회전 |
| 정규화 | 픽셀 값을 평균 0 근처로 | train 통계 사용 |
| skip connection | 인코더 정보를 디코더로 직접 전달 | 7픽셀 종양 검출에 필수 |
| 손실함수 | 얼마나 틀렸는지를 숫자로 | BCE + Dice |
| Dice | 두 영역이 겹치는 정도 (0~1) | 주 지표 |
| IoU | Dice와 비슷하나 더 엄격 | 보조 지표 |
| 임계값 | 확률 몇부터 종양이라 할지 | val에서 결정 |
| recall (재현율) | 놓치지 않은 비율 | 임상 최우선 |
| precision (정밀도) | 오탐이 아닌 비율 | 경보 피로 관련 |
| 과적합 | 학습 데이터를 외워버림 | M1 격차 +0.1459 |
| 체크포인트 | 가장 좋았던 가중치 저장 | val Dice 기준 |
| AMP | 계산을 float16으로 (빠름) | 1.5~2배 가속 |
| 대응표본 | 같은 대상을 두 조건으로 비교 | 환자 16명 |
| p값 | 우연일 확률 | 0.05 미만이면 유의 |
